"""
Zenic-Flijo API v2 — Autenticacion y Autorizacion
===================================================

Sistema de autenticacion y autorizacion para la API v2:
- APIKeyAuth: Valida X-API-Key contra la base de datos
- BearerTokenAuth: Valida tokens JWT/Bearer
- get_current_user: Retorna el usuario autenticado
- require_permission: Verifica permisos RBAC granulares
- get_tenant: Resuelve el tenant desde la solicitud
- Rate limiting por API key usando RedisService

Integra con el sistema RBAC existente en src.security.rbac.
"""

from __future__ import annotations

import hashlib
import hmac
import json
import os
import time
import uuid
from typing import Any

from fastapi import Depends, HTTPException, Request, Security, status
from fastapi.security import APIKeyHeader, HTTPAuthorizationCredentials, HTTPBearer

from src.core.repositories import AuditRepository
from src.core.db import DatabaseManager
from src.core.db import RedisService
from src.core.repositories import SettingsRepository
from src.core.repositories import UserRepository
from src.core.security.rbac import RBACManager
from src.tenant.service import TenantService
from src.core.logging import setup_logging

logger = setup_logging(__name__)

# ── Constantes ─────────────────────────────────────────────────

_API_KEY_PREFIX = "zf_"
_JWT_SECRET_ENV = "WFD_API_V2_JWT_SECRET"
# Mínimo de caracteres para considerar el secreto aceptable.
# 64 chars base64 ≈ 384 bits de entropía (suficiente para HMAC-SHA256).
_JWT_SECRET_MIN_LEN = 64
_RATE_LIMIT_WINDOW = 60  # segundos
_RATE_LIMIT_MAX_REQUESTS = 100  # requests por ventana

# ── Security Schemes ───────────────────────────────────────────

api_key_header = APIKeyHeader(name="X-API-Key", auto_error=False)
bearer_scheme = HTTPBearer(auto_error=False)


# ── Token Helpers ──────────────────────────────────────────────

# Cache del secret en modo dev para que generate_token/validate_token usen
# el mismo valor dentro de una sesión. En producción el secret viene de env
# var y el cache es estable.
_CACHED_DEV_SECRET: str | None = None


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__get_jwt_secret__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_jwt_secret__mutmut)
def _get_jwt_secret() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_orig() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_1() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = None
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_2() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(None, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_3() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, None)
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_4() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get("")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_5() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, )
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_6() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "XXXX")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_7() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = None

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_8() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").upper() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_9() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get(None, "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_10() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", None).lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_11() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_12() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", ).lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_13() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("XXWFD_PRODUCTIONXX", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_14() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("wfd_production", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_15() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "XXfalseXX").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_16() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "FALSE").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_17() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() != "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_18() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "XXtrueXX"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_19() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "TRUE"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_20() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret or len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_21() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) > _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_22() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            None
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_23() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "XXSEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto XX"
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_24() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "seguridad crítica: wfd_api_v2_jwt_secret no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_25() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET NO CONFIGURADO O DEMASIADO CORTO "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_26() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "XXpython3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  XX"
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_27() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "PYTHON3 -C \"IMPORT SECRETS; PRINT(SECRETS.TOKEN_URLSAFE(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_28() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "XXy configúrelo en su entorno antes de desplegar.XX"
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_29() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "Y CONFIGÚRELO EN SU ENTORNO ANTES DE DESPLEGAR."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_30() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_31() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_32() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = None
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_33() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(None)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_34() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(49)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_35() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            None,
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_36() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=None,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_37() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_38() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_39() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "XXWFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio XX"
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_40() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "wfd_api_v2_jwt_secret no configurado. se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_41() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET NO CONFIGURADO. SE GENERÓ UN SECRETO ALEATORIO "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_42() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "XXefímero para esta sesión. NO use esto en producción. Configure la XX"
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_43() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. no use esto en producción. configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_44() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "EFÍMERO PARA ESTA SESIÓN. NO USE ESTO EN PRODUCCIÓN. CONFIGURE LA "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_45() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "XXvariable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de XX"
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_46() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno wfd_api_v2_jwt_secret con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_47() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "VARIABLE DE ENTORNO WFD_API_V2_JWT_SECRET CON UN VALOR ALEATORIO DE "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_48() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=3,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_49() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        None,
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_50() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=None,
    )
    return secret


def x__get_jwt_secret__mutmut_51() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        stacklevel=2,
    )
    return secret


def x__get_jwt_secret__mutmut_52() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        )
    return secret


def x__get_jwt_secret__mutmut_53() -> str:
    """Obtiene el secreto JWT desde la variable de entorno.

    CRÍTICO: No hay default. La app DEBE configurar WFD_API_V2_JWT_SECRET
    con un valor aleatorio de al menos 64 caracteres. En producción, si no
    está configurado o es demasiado corto, se lanza RuntimeError para evitar
    arrancar con un secreto débil o inexistente (que permitiría a cualquier
    atacante con acceso al código forjar tokens JWT).

    En desarrollo (WFD_PRODUCTION != "true"), si no está configurado se
    genera uno aleatorio por sesión (cacheado para que generate_token y
    validate_token usen el mismo valor) y se emite un warning.
    """
    global _CACHED_DEV_SECRET

    secret = os.environ.get(_JWT_SECRET_ENV, "")
    production = os.environ.get("WFD_PRODUCTION", "false").lower() == "true"

    if secret and len(secret) >= _JWT_SECRET_MIN_LEN:
        # Secret válido configurado vía env var — usarlo siempre (sin cachear
        # porque podría cambiar en runtime en tests).
        return secret

    if production:
        raise RuntimeError(
            "SEGURIDAD CRÍTICA: WFD_API_V2_JWT_SECRET no configurado o demasiado corto "
            f"(mínimo {_JWT_SECRET_MIN_LEN} caracteres). Genere uno nuevo con, por ejemplo:  "
            "python3 -c \"import secrets; print(secrets.token_urlsafe(48))\"  "
            "y configúrelo en su entorno antes de desplegar."
        )

    # Modo desarrollo: usar el secret cacheado si existe (estabilidad
    # dentro de la sesión). Si no, generar uno nuevo y cachearlo.
    if _CACHED_DEV_SECRET is not None:
        return _CACHED_DEV_SECRET

    if not secret:
        import secrets as _secrets
        import warnings
        _CACHED_DEV_SECRET = _secrets.token_urlsafe(48)
        warnings.warn(
            "WFD_API_V2_JWT_SECRET no configurado. Se generó un secreto aleatorio "
            "efímero para esta sesión. NO use esto en producción. Configure la "
            "variable de entorno WFD_API_V2_JWT_SECRET con un valor aleatorio de "
            f"al menos {_JWT_SECRET_MIN_LEN} caracteres.",
            stacklevel=2,
        )
        return _CACHED_DEV_SECRET

    # Secret configurado pero demasiado corto — advertir pero permitir en dev.
    import warnings
    warnings.warn(
        f"WFD_API_V2_JWT_SECRET tiene {len(secret)} caracteres; se recomiendan "
        f"al menos {_JWT_SECRET_MIN_LEN}. Considere rotarlo por uno más largo.",
        stacklevel=3,
    )
    return secret

mutants_x__get_jwt_secret__mutmut['_mutmut_orig'] = x__get_jwt_secret__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_1'] = x__get_jwt_secret__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_2'] = x__get_jwt_secret__mutmut_2 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_3'] = x__get_jwt_secret__mutmut_3 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_4'] = x__get_jwt_secret__mutmut_4 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_5'] = x__get_jwt_secret__mutmut_5 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_6'] = x__get_jwt_secret__mutmut_6 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_7'] = x__get_jwt_secret__mutmut_7 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_8'] = x__get_jwt_secret__mutmut_8 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_9'] = x__get_jwt_secret__mutmut_9 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_10'] = x__get_jwt_secret__mutmut_10 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_11'] = x__get_jwt_secret__mutmut_11 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_12'] = x__get_jwt_secret__mutmut_12 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_13'] = x__get_jwt_secret__mutmut_13 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_14'] = x__get_jwt_secret__mutmut_14 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_15'] = x__get_jwt_secret__mutmut_15 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_16'] = x__get_jwt_secret__mutmut_16 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_17'] = x__get_jwt_secret__mutmut_17 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_18'] = x__get_jwt_secret__mutmut_18 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_19'] = x__get_jwt_secret__mutmut_19 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_20'] = x__get_jwt_secret__mutmut_20 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_21'] = x__get_jwt_secret__mutmut_21 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_22'] = x__get_jwt_secret__mutmut_22 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_23'] = x__get_jwt_secret__mutmut_23 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_24'] = x__get_jwt_secret__mutmut_24 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_25'] = x__get_jwt_secret__mutmut_25 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_26'] = x__get_jwt_secret__mutmut_26 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_27'] = x__get_jwt_secret__mutmut_27 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_28'] = x__get_jwt_secret__mutmut_28 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_29'] = x__get_jwt_secret__mutmut_29 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_30'] = x__get_jwt_secret__mutmut_30 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_31'] = x__get_jwt_secret__mutmut_31 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_32'] = x__get_jwt_secret__mutmut_32 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_33'] = x__get_jwt_secret__mutmut_33 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_34'] = x__get_jwt_secret__mutmut_34 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_35'] = x__get_jwt_secret__mutmut_35 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_36'] = x__get_jwt_secret__mutmut_36 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_37'] = x__get_jwt_secret__mutmut_37 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_38'] = x__get_jwt_secret__mutmut_38 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_39'] = x__get_jwt_secret__mutmut_39 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_40'] = x__get_jwt_secret__mutmut_40 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_41'] = x__get_jwt_secret__mutmut_41 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_42'] = x__get_jwt_secret__mutmut_42 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_43'] = x__get_jwt_secret__mutmut_43 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_44'] = x__get_jwt_secret__mutmut_44 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_45'] = x__get_jwt_secret__mutmut_45 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_46'] = x__get_jwt_secret__mutmut_46 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_47'] = x__get_jwt_secret__mutmut_47 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_48'] = x__get_jwt_secret__mutmut_48 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_49'] = x__get_jwt_secret__mutmut_49 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_50'] = x__get_jwt_secret__mutmut_50 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_51'] = x__get_jwt_secret__mutmut_51 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_52'] = x__get_jwt_secret__mutmut_52 # type: ignore # mutmut generated
mutants_x__get_jwt_secret__mutmut['x__get_jwt_secret__mutmut_53'] = x__get_jwt_secret__mutmut_53 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_generate_token__mutmut)
def generate_token(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_orig(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_1(payload: dict[str, Any], expires_in: int = 3601) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_2(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = None
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_3(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "XXpayloadXX": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_4(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "PAYLOAD": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_5(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "XXiatXX": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_6(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "IAT": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_7(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(None),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_8(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "XXexpXX": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_9(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "EXP": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_10(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) - expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_11(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(None) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_12(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "XXjtiXX": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_13(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "JTI": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_14(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(None),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_15(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = None
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_16(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(None, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_17(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=None, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_18(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=None)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_19(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_20(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_21(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, )
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_22(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=False, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_23(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = None

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_24(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(None).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_25(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = None

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_26(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(None, token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_27(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), None, hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_28(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), None).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_29(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(token_b64.encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_30(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), hashlib.sha256).hexdigest()

    return f"{token_b64}.{signature}"


def x_generate_token__mutmut_31(payload: dict[str, Any], expires_in: int = 3600) -> str:
    """Genera un token JWT simplificado ( HMAC-based ).

    Args:
        payload: Datos a incluir en el token
        expires_in: Tiempo de vida en segundos (default: 1 hora)

    Returns:
        Token como string codificado en base64
    """
    import base64

    token_data = {
        "payload": payload,
        "iat": int(time.time()),
        "exp": int(time.time()) + expires_in,
        "jti": str(uuid.uuid4()),
    }
    token_json = json.dumps(token_data, sort_keys=True, default=str)
    token_b64 = base64.urlsafe_b64encode(token_json.encode()).decode()

    # Firma HMAC-SHA256
    signature = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), ).hexdigest()

    return f"{token_b64}.{signature}"

mutants_x_generate_token__mutmut['_mutmut_orig'] = x_generate_token__mutmut_orig # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_1'] = x_generate_token__mutmut_1 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_2'] = x_generate_token__mutmut_2 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_3'] = x_generate_token__mutmut_3 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_4'] = x_generate_token__mutmut_4 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_5'] = x_generate_token__mutmut_5 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_6'] = x_generate_token__mutmut_6 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_7'] = x_generate_token__mutmut_7 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_8'] = x_generate_token__mutmut_8 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_9'] = x_generate_token__mutmut_9 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_10'] = x_generate_token__mutmut_10 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_11'] = x_generate_token__mutmut_11 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_12'] = x_generate_token__mutmut_12 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_13'] = x_generate_token__mutmut_13 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_14'] = x_generate_token__mutmut_14 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_15'] = x_generate_token__mutmut_15 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_16'] = x_generate_token__mutmut_16 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_17'] = x_generate_token__mutmut_17 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_18'] = x_generate_token__mutmut_18 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_19'] = x_generate_token__mutmut_19 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_20'] = x_generate_token__mutmut_20 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_21'] = x_generate_token__mutmut_21 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_22'] = x_generate_token__mutmut_22 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_23'] = x_generate_token__mutmut_23 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_24'] = x_generate_token__mutmut_24 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_25'] = x_generate_token__mutmut_25 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_26'] = x_generate_token__mutmut_26 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_27'] = x_generate_token__mutmut_27 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_28'] = x_generate_token__mutmut_28 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_29'] = x_generate_token__mutmut_29 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_30'] = x_generate_token__mutmut_30 # type: ignore # mutmut generated
mutants_x_generate_token__mutmut['x_generate_token__mutmut_31'] = x_generate_token__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_validate_token__mutmut)
def validate_token(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_orig(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_1(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = None
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_2(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(None)
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_3(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split("XX.XX")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_4(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) == 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_5(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 3:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_6(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = None

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_7(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = None
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_8(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(None, token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_9(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), None, hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_10(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), None).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_11(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_12(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_13(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), ).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_14(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_15(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(None, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_16(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, None):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_17(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_18(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, ):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_19(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = None
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_20(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(None).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_21(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = None
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_22(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(None)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_23(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get(None, 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_24(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", None) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_25(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get(0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_26(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", ) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_27(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("XXexpXX", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_28(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("EXP", 0) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_29(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 1) < int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_30(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) <= int(time.time()):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_31(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(None):
        return None

    return token_data.get("payload")


def x_validate_token__mutmut_32(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get(None)


def x_validate_token__mutmut_33(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("XXpayloadXX")


def x_validate_token__mutmut_34(token: str) -> dict[str, Any] | None:
    """Valida un token JWT simplificado (HMAC-based).

    Args:
        token: Token a validar

    Returns:
        Payload del token si es valido, None si es invalido o expirado
    """
    import base64

    parts = token.split(".")
    if len(parts) != 2:
        return None

    token_b64, signature = parts

    # Verificar firma
    expected_sig = hmac.new(_get_jwt_secret().encode(), token_b64.encode(), hashlib.sha256).hexdigest()
    if not hmac.compare_digest(signature, expected_sig):
        return None

    try:
        token_json = base64.urlsafe_b64decode(token_b64.encode()).decode()
        token_data = json.loads(token_json)
    except (ValueError, json.JSONDecodeError):
        return None

    # Verificar expiracion
    if token_data.get("exp", 0) < int(time.time()):
        return None

    return token_data.get("PAYLOAD")

mutants_x_validate_token__mutmut['_mutmut_orig'] = x_validate_token__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_1'] = x_validate_token__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_2'] = x_validate_token__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_3'] = x_validate_token__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_4'] = x_validate_token__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_5'] = x_validate_token__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_6'] = x_validate_token__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_7'] = x_validate_token__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_8'] = x_validate_token__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_9'] = x_validate_token__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_10'] = x_validate_token__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_11'] = x_validate_token__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_12'] = x_validate_token__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_13'] = x_validate_token__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_14'] = x_validate_token__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_15'] = x_validate_token__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_16'] = x_validate_token__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_17'] = x_validate_token__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_18'] = x_validate_token__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_19'] = x_validate_token__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_20'] = x_validate_token__mutmut_20 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_21'] = x_validate_token__mutmut_21 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_22'] = x_validate_token__mutmut_22 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_23'] = x_validate_token__mutmut_23 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_24'] = x_validate_token__mutmut_24 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_25'] = x_validate_token__mutmut_25 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_26'] = x_validate_token__mutmut_26 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_27'] = x_validate_token__mutmut_27 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_28'] = x_validate_token__mutmut_28 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_29'] = x_validate_token__mutmut_29 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_30'] = x_validate_token__mutmut_30 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_31'] = x_validate_token__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_32'] = x_validate_token__mutmut_32 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_33'] = x_validate_token__mutmut_33 # type: ignore # mutmut generated
mutants_x_validate_token__mutmut['x_validate_token__mutmut_34'] = x_validate_token__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAPIKeyAuthǁ__call____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut: MutantDict = {}  # type: ignore


# ── API Key Authentication ─────────────────────────────────────


class APIKeyAuth:
    """Dependencia que valida la API key del header X-API-Key."""

    @_mutmut_mutated(mutants_xǁAPIKeyAuthǁ__init____mutmut)
    def __init__(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_orig(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_1(self) -> None:
        self._db = None
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_2(self) -> None:
        self._db = DatabaseManager()
        self._redis = None
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_3(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = None
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_4(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(None)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_5(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = None
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_6(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(None)
        self._audit = AuditRepository(self._db)

    def xǁAPIKeyAuthǁ__init____mutmut_7(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = None

    def xǁAPIKeyAuthǁ__init____mutmut_8(self) -> None:
        self._db = DatabaseManager()
        self._redis = RedisService()
        self._users = UserRepository(self._db)
        self._settings = SettingsRepository(self._db)
        self._audit = AuditRepository(None)

    @_mutmut_mutated(mutants_xǁAPIKeyAuthǁ__call____mutmut)
    async def __call__(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_orig(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_1(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_2(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=None,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_3(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail=None,
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_4(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_5(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_6(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="XXAPI key requerida. Envie el header X-API-Key.XX",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_7(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="api key requerida. envie el header x-api-key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_8(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API KEY REQUERIDA. ENVIE EL HEADER X-API-KEY.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_9(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_10(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(None):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_11(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=None,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_12(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail=None,
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_13(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_14(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_15(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="XXFormato de API key invalido.XX",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_16(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="formato de api key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_17(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="FORMATO DE API KEY INVALIDO.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_18(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = None
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_19(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:9]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_20(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(None).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_21(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:17]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_22(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = None
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_23(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(None)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_24(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") or time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_25(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get(None) and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_26(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("XXexpires_atXX") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_27(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("EXPIRES_AT") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_28(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() >= cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_29(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["XXexpires_atXX"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_30(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["EXPIRES_AT"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_31(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(None)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_32(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=None,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_33(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail=None,
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_34(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_35(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_36(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="XXAPI key expirada.XX",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_37(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="api key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_38(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API KEY EXPIRADA.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_39(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(None, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_40(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, None)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_41(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_42(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, )
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_43(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = None
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_44(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(None).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_45(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = None

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_46(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone(None, (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_47(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", None)

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_48(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone((key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_49(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", )

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_50(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("XXSELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1XX", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_51(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("select * from api_keys where key_hash = ? and is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_52(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM API_KEYS WHERE KEY_HASH = ? AND IS_ACTIVE = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_53(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_54(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=None,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_55(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail=None,
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_56(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_57(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_58(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="XXAPI key invalida o revocada.XX",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_59(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="api key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_60(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API KEY INVALIDA O REVOCADA.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_61(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get(None):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_62(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("XXexpires_atXX"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_63(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("EXPIRES_AT"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_64(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = None
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_65(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(None)
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_66(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(None))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_67(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["XXexpires_atXX"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_68(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["EXPIRES_AT"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_69(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() <= time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_70(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=None,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_71(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail=None,
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_72(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_73(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_74(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="XXAPI key expirada.XX",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_75(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="api key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_76(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API KEY EXPIRADA.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_77(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = None
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_78(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "XXidXX": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_79(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "ID": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_80(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["XXidXX"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_81(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["ID"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_82(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "XXnameXX": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_83(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "NAME": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_84(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get(None, ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_85(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", None),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_86(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get(""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_87(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_88(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("XXnameXX", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_89(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("NAME", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_90(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", "XXXX"),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_91(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "XXuser_idXX": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_92(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "USER_ID": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_93(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get(None),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_94(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("XXuser_idXX"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_95(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("USER_ID"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_96(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "XXtenant_idXX": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_97(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "TENANT_ID": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_98(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get(None),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_99(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("XXtenant_idXX"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_100(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("TENANT_ID"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_101(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "XXscopesXX": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_102(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "SCOPES": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_103(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(None),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_104(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get(None, "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_105(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", None)),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_106(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_107(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", )),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_108(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("XXscopesXX", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_109(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("SCOPES", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_110(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "XX[]XX")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_111(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "XXexpires_atXX": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_112(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "EXPIRES_AT": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_113(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get(None):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_114(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("XXexpires_atXX"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_115(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("EXPIRES_AT"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_116(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = None
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_117(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["XXexpires_atXX"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_118(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["EXPIRES_AT"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_119(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(None).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_120(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(None)).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_121(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["XXexpires_atXX"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_122(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["EXPIRES_AT"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_123(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(None, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_124(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, None, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_125(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=None)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_126(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_127(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_128(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, )

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_129(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=301)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_130(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute(None, (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_131(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", None)
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_132(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute((key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_133(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", )
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_134(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("XXUPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?XX", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_135(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("update api_keys set last_used_at = current_timestamp where id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_136(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE API_KEYS SET LAST_USED_AT = CURRENT_TIMESTAMP WHERE ID = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_137(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["XXidXX"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_138(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["ID"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_139(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(None, key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_140(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, None)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_141(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(key_info)

        return key_info

    async def xǁAPIKeyAuthǁ__call____mutmut_142(self, request: Request, api_key: str = Security(api_key_header)) -> dict[str, Any]:
        """Valida la API key contra la base de datos y aplica rate limiting.

        Args:
            request: Solicitud HTTP
            api_key: API key del header X-API-Key

        Returns:
            Dict con datos de la API key validada

        Raises:
            HTTPException: Si la API key es invalida, expirada o rate limited
        """
        if not api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key requerida. Envie el header X-API-Key.",
            )

        # Verificar formato
        if not api_key.startswith(_API_KEY_PREFIX):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Formato de API key invalido.",
            )

        # Buscar en cache Redis primero
        cache_key = f"api_key:{api_key[:8]}:{hashlib.sha256(api_key.encode()).hexdigest()[:16]}"
        cached = self._redis.get_json(cache_key)
        if cached:
            # Verificar expiracion desde cache
            if cached.get("expires_at") and time.time() > cached["expires_at"]:
                self._redis.delete(cache_key)
                raise HTTPException(
                    status_code=status.HTTP_401_UNAUTHORIZED,
                    detail="API key expirada.",
                )
            self._check_rate_limit(api_key, cached)
            return cached

        # Buscar en base de datos
        key_hash = hashlib.sha256(api_key.encode()).hexdigest()
        key_data = self._db.fetchone("SELECT * FROM api_keys WHERE key_hash = ? AND is_active = 1", (key_hash,))

        if not key_data:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="API key invalida o revocada.",
            )

        # Verificar expiracion
        if key_data.get("expires_at"):
            from datetime import datetime

            try:
                exp = datetime.fromisoformat(str(key_data["expires_at"]))
                if exp.timestamp() < time.time():
                    raise HTTPException(
                        status_code=status.HTTP_401_UNAUTHORIZED,
                        detail="API key expirada.",
                    )
            except (ValueError, TypeError):
                pass

        # Construir datos de la API key
        key_info = {
            "id": key_data["id"],
            "name": key_data.get("name", ""),
            "user_id": key_data.get("user_id"),
            "tenant_id": key_data.get("tenant_id"),
            "scopes": json.loads(key_data.get("scopes", "[]")),
            "expires_at": None,
        }
        if key_data.get("expires_at"):
            try:
                from datetime import datetime

                key_info["expires_at"] = datetime.fromisoformat(str(key_data["expires_at"])).timestamp()
            except (ValueError, TypeError):
                pass

        # Cachear en Redis (5 minutos)
        self._redis.set_json(cache_key, key_info, ttl=300)

        # Actualizar last_used_at
        self._db.execute("UPDATE api_keys SET last_used_at = CURRENT_TIMESTAMP WHERE id = ?", (key_data["id"],))
        self._db.commit()

        # Rate limiting
        self._check_rate_limit(api_key, )

        return key_info

    @_mutmut_mutated(mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut)
    def _check_rate_limit(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_orig(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_1(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = None
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_2(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:9]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_3(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = None
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_4(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(None, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_5(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, None, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_6(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, None)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_7(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(_RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_8(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_9(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, )
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_10(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_11(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get(None, True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_12(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", None):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_13(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get(True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_14(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", ):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_15(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("XXallowedXX", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_16(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("ALLOWED", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_17(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", False):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_18(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=None,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_19(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=None,
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_20(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers=None,
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_21(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_22(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                headers={"Retry-After": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_23(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_24(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"XXRetry-AfterXX": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_25(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"retry-after": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_26(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"RETRY-AFTER": str(result.get("reset_at", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_27(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(None)},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_28(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get(None, _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_29(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", None))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_30(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get(_RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_31(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("reset_at", ))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_32(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("XXreset_atXX", _RATE_LIMIT_WINDOW))},
            )

    def xǁAPIKeyAuthǁ_check_rate_limit__mutmut_33(self, api_key: str, key_info: dict[str, Any]) -> None:
        """Verifica el rate limit para la API key.

        Args:
            api_key: API key completa (para el identificador de rate limit)
            key_info: Informacion de la API key

        Raises:
            HTTPException: Si se excede el rate limit
        """
        rate_key = f"api_v2:{api_key[:8]}"
        result = self._redis.check_rate_limit(rate_key, _RATE_LIMIT_MAX_REQUESTS, _RATE_LIMIT_WINDOW)
        if not result.get("allowed", True):
            raise HTTPException(
                status_code=status.HTTP_429_TOO_MANY_REQUESTS,
                detail=f"Rate limit excedido. Intentelo de nuevo en {_RATE_LIMIT_WINDOW} segundos.",
                headers={"Retry-After": str(result.get("RESET_AT", _RATE_LIMIT_WINDOW))},
            )

mutants_xǁAPIKeyAuthǁ__init____mutmut['_mutmut_orig'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_1'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_2'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_3'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_4'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_5'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_6'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_7'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__init____mutmut['xǁAPIKeyAuthǁ__init____mutmut_8'] = APIKeyAuth.xǁAPIKeyAuthǁ__init____mutmut_8 # type: ignore # mutmut generated

mutants_xǁAPIKeyAuthǁ__call____mutmut['_mutmut_orig'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_1'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_2'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_2 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_3'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_3 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_4'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_4 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_5'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_5 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_6'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_6 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_7'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_7 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_8'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_8 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_9'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_9 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_10'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_10 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_11'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_11 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_12'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_12 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_13'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_13 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_14'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_14 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_15'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_15 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_16'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_16 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_17'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_17 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_18'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_18 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_19'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_19 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_20'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_20 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_21'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_21 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_22'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_22 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_23'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_23 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_24'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_24 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_25'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_25 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_26'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_26 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_27'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_27 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_28'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_28 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_29'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_29 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_30'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_30 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_31'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_31 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_32'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_32 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_33'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_33 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_34'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_34 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_35'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_35 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_36'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_36 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_37'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_37 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_38'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_38 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_39'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_39 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_40'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_40 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_41'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_41 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_42'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_42 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_43'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_43 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_44'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_44 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_45'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_45 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_46'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_46 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_47'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_47 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_48'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_48 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_49'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_49 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_50'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_50 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_51'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_51 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_52'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_52 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_53'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_53 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_54'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_54 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_55'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_55 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_56'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_56 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_57'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_57 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_58'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_58 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_59'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_59 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_60'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_60 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_61'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_61 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_62'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_62 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_63'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_63 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_64'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_64 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_65'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_65 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_66'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_66 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_67'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_67 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_68'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_68 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_69'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_69 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_70'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_70 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_71'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_71 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_72'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_72 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_73'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_73 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_74'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_74 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_75'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_75 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_76'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_76 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_77'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_77 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_78'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_78 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_79'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_79 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_80'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_80 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_81'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_81 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_82'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_82 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_83'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_83 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_84'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_84 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_85'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_85 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_86'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_86 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_87'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_87 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_88'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_88 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_89'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_89 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_90'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_90 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_91'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_91 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_92'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_92 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_93'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_93 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_94'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_94 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_95'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_95 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_96'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_96 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_97'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_97 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_98'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_98 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_99'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_99 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_100'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_100 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_101'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_101 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_102'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_102 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_103'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_103 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_104'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_104 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_105'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_105 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_106'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_106 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_107'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_107 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_108'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_108 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_109'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_109 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_110'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_110 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_111'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_111 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_112'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_112 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_113'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_113 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_114'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_114 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_115'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_115 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_116'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_116 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_117'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_117 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_118'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_118 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_119'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_119 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_120'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_120 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_121'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_121 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_122'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_122 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_123'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_123 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_124'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_124 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_125'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_125 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_126'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_126 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_127'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_127 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_128'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_128 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_129'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_129 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_130'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_130 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_131'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_131 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_132'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_132 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_133'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_133 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_134'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_134 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_135'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_135 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_136'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_136 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_137'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_137 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_138'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_138 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_139'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_139 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_140'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_140 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_141'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_141 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ__call____mutmut['xǁAPIKeyAuthǁ__call____mutmut_142'] = APIKeyAuth.xǁAPIKeyAuthǁ__call____mutmut_142 # type: ignore # mutmut generated

mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['_mutmut_orig'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_1'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_2'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_3'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_4'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_5'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_6'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_7'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_8'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_9'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_10'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_11'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_12'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_13'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_14'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_15'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_16'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_17'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_18'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_19'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_20'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_21'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_22'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_23'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_24'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_25'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_26'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_27'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_28'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_29'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_30'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_31'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_32'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAPIKeyAuthǁ_check_rate_limit__mutmut['xǁAPIKeyAuthǁ_check_rate_limit__mutmut_33'] = APIKeyAuth.xǁAPIKeyAuthǁ_check_rate_limit__mutmut_33 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut: MutantDict = {}  # type: ignore


# ── Bearer Token Authentication ────────────────────────────────


class BearerTokenAuth:
    """Dependencia que valida tokens Bearer JWT."""

    @_mutmut_mutated(mutants_xǁBearerTokenAuthǁ__call____mutmut)
    async def __call__(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_orig(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_1(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_2(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = None
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_3(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(None)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_4(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_5(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=None,
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_6(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail=None,
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_7(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                detail="Token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_8(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_9(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="XXToken invalido o expirado.XX",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_10(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="token invalido o expirado.",
            )

        return payload

    async def xǁBearerTokenAuthǁ__call____mutmut_11(
        self, credentials: HTTPAuthorizationCredentials = Security(bearer_scheme)
    ) -> dict[str, Any] | None:
        """Valida el token Bearer JWT.

        Args:
            credentials: Credenciales del header Authorization

        Returns:
            Payload del token si es valido

        Raises:
            HTTPException: Si el token es invalido o expirado
        """
        if not credentials:
            return None

        payload = validate_token(credentials.credentials)
        if not payload:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="TOKEN INVALIDO O EXPIRADO.",
            )

        return payload

mutants_xǁBearerTokenAuthǁ__call____mutmut['_mutmut_orig'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_orig # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_1'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_1 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_2'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_2 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_3'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_3 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_4'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_4 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_5'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_5 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_6'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_6 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_7'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_7 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_8'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_8 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_9'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_9 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_10'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_10 # type: ignore # mutmut generated
mutants_xǁBearerTokenAuthǁ__call____mutmut['xǁBearerTokenAuthǁ__call____mutmut_11'] = BearerTokenAuth.xǁBearerTokenAuthǁ__call____mutmut_11 # type: ignore # mutmut generated


# ── Combined Auth Dependencies ─────────────────────────────────

_api_key_auth = APIKeyAuth()
_bearer_auth = BearerTokenAuth()
mutants_x_get_current_user__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_current_user__mutmut)
async def get_current_user(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_orig(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_1(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info or api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_2(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get(None):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_3(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("XXuser_idXX"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_4(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("USER_ID"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_5(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = None
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_6(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = None
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_7(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(None)
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_8(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["XXuser_idXX"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_9(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["USER_ID"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_10(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user or user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_11(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get(None):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_12(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("XXis_activeXX"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_13(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("IS_ACTIVE"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_14(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "XXuser_idXX": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_15(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "USER_ID": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_16(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["XXidXX"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_17(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["ID"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_18(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "XXusernameXX": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_19(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "USERNAME": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_20(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get(None, ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_21(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", None),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_22(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get(""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_23(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_24(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("XXusernameXX", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_25(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("USERNAME", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_26(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", "XXXX"),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_27(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "XXroleXX": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_28(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "ROLE": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_29(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get(None, "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_30(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", None),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_31(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_32(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", ),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_33(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("XXroleXX", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_34(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("ROLE", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_35(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "XXviewerXX"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_36(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "VIEWER"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_37(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "XXtenant_idXX": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_38(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "TENANT_ID": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_39(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get(None),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_40(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("XXtenant_idXX"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_41(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("TENANT_ID"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_42(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "XXscopesXX": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_43(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "SCOPES": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_44(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get(None, []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_45(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", None),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_46(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get([]),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_47(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", ),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_48(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("XXscopesXX", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_49(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("SCOPES", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_50(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "XXauth_methodXX": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_51(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "AUTH_METHOD": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_52(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "XXapi_keyXX",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_53(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "API_KEY",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_54(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "XXuser_idXX": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_55(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "USER_ID": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_56(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get(None, 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_57(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", None),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_58(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get(0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_59(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", ),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_60(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("XXuser_idXX", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_61(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("USER_ID", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_62(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 1),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_63(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "XXusernameXX": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_64(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "USERNAME": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_65(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get(None, ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_66(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", None),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_67(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get(""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_68(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_69(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("XXusernameXX", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_70(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("USERNAME", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_71(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", "XXXX"),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_72(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "XXroleXX": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_73(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "ROLE": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_74(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get(None, "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_75(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", None),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_76(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_77(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", ),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_78(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("XXroleXX", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_79(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("ROLE", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_80(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "XXviewerXX"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_81(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "VIEWER"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_82(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "XXtenant_idXX": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_83(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "TENANT_ID": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_84(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get(None),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_85(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("XXtenant_idXX"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_86(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("TENANT_ID"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_87(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "XXscopesXX": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_88(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "SCOPES": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_89(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get(None, []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_90(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", None),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_91(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get([]),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_92(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", ),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_93(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("XXscopesXX", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_94(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("SCOPES", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_95(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "XXauth_methodXX": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_96(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "AUTH_METHOD": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_97(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "XXbearerXX",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_98(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "BEARER",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_99(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=None,
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_100(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail=None,
    )


async def x_get_current_user__mutmut_101(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        detail="Autenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.",
    )


async def x_get_current_user__mutmut_102(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        )


async def x_get_current_user__mutmut_103(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="XXAutenticacion requerida. Proporcione X-API-Key o Authorization: Bearer <token>.XX",
    )


async def x_get_current_user__mutmut_104(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="autenticacion requerida. proporcione x-api-key o authorization: bearer <token>.",
    )


async def x_get_current_user__mutmut_105(
    request: Request,
    api_key_info: dict[str, Any] | None = Depends(_api_key_auth.__call__),
    bearer_payload: dict[str, Any] | None = Depends(_bearer_auth.__call__),
) -> dict[str, Any]:
    """Obtiene el usuario autenticado desde API key o Bearer token.

    Intenta primero API key, luego Bearer token. Al menos uno debe ser valido.

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (inyectado por APIKeyAuth)
        bearer_payload: Payload del Bearer token (inyectado por BearerTokenAuth)

    Returns:
        Dict con datos del usuario autenticado

    Raises:
        HTTPException: Si no se proporciona ninguna credencial valida
    """
    # Intentar API key primero
    if api_key_info and api_key_info.get("user_id"):
        users = UserRepository()
        user = users.get_user(api_key_info["user_id"])
        if user and user.get("is_active"):
            return {
                "user_id": user["id"],
                "username": user.get("username", ""),
                "role": user.get("role", "viewer"),
                "tenant_id": api_key_info.get("tenant_id"),
                "scopes": api_key_info.get("scopes", []),
                "auth_method": "api_key",
            }

    # Intentar Bearer token
    if bearer_payload:
        return {
            "user_id": bearer_payload.get("user_id", 0),
            "username": bearer_payload.get("username", ""),
            "role": bearer_payload.get("role", "viewer"),
            "tenant_id": bearer_payload.get("tenant_id"),
            "scopes": bearer_payload.get("scopes", []),
            "auth_method": "bearer",
        }

    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail="AUTENTICACION REQUERIDA. PROPORCIONE X-API-KEY O AUTHORIZATION: BEARER <TOKEN>.",
    )

mutants_x_get_current_user__mutmut['_mutmut_orig'] = x_get_current_user__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_1'] = x_get_current_user__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_2'] = x_get_current_user__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_3'] = x_get_current_user__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_4'] = x_get_current_user__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_5'] = x_get_current_user__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_6'] = x_get_current_user__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_7'] = x_get_current_user__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_8'] = x_get_current_user__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_9'] = x_get_current_user__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_10'] = x_get_current_user__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_11'] = x_get_current_user__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_12'] = x_get_current_user__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_13'] = x_get_current_user__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_14'] = x_get_current_user__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_15'] = x_get_current_user__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_16'] = x_get_current_user__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_17'] = x_get_current_user__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_18'] = x_get_current_user__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_19'] = x_get_current_user__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_20'] = x_get_current_user__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_21'] = x_get_current_user__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_22'] = x_get_current_user__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_23'] = x_get_current_user__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_24'] = x_get_current_user__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_25'] = x_get_current_user__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_26'] = x_get_current_user__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_27'] = x_get_current_user__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_28'] = x_get_current_user__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_29'] = x_get_current_user__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_30'] = x_get_current_user__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_31'] = x_get_current_user__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_32'] = x_get_current_user__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_33'] = x_get_current_user__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_34'] = x_get_current_user__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_35'] = x_get_current_user__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_36'] = x_get_current_user__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_37'] = x_get_current_user__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_38'] = x_get_current_user__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_39'] = x_get_current_user__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_40'] = x_get_current_user__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_41'] = x_get_current_user__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_42'] = x_get_current_user__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_43'] = x_get_current_user__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_44'] = x_get_current_user__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_45'] = x_get_current_user__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_46'] = x_get_current_user__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_47'] = x_get_current_user__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_48'] = x_get_current_user__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_49'] = x_get_current_user__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_50'] = x_get_current_user__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_51'] = x_get_current_user__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_52'] = x_get_current_user__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_53'] = x_get_current_user__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_54'] = x_get_current_user__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_55'] = x_get_current_user__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_56'] = x_get_current_user__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_57'] = x_get_current_user__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_58'] = x_get_current_user__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_59'] = x_get_current_user__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_60'] = x_get_current_user__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_61'] = x_get_current_user__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_62'] = x_get_current_user__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_63'] = x_get_current_user__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_64'] = x_get_current_user__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_65'] = x_get_current_user__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_66'] = x_get_current_user__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_67'] = x_get_current_user__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_68'] = x_get_current_user__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_69'] = x_get_current_user__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_70'] = x_get_current_user__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_71'] = x_get_current_user__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_72'] = x_get_current_user__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_73'] = x_get_current_user__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_74'] = x_get_current_user__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_75'] = x_get_current_user__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_76'] = x_get_current_user__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_77'] = x_get_current_user__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_78'] = x_get_current_user__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_79'] = x_get_current_user__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_80'] = x_get_current_user__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_81'] = x_get_current_user__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_82'] = x_get_current_user__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_83'] = x_get_current_user__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_84'] = x_get_current_user__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_85'] = x_get_current_user__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_86'] = x_get_current_user__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_87'] = x_get_current_user__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_88'] = x_get_current_user__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_89'] = x_get_current_user__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_90'] = x_get_current_user__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_91'] = x_get_current_user__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_92'] = x_get_current_user__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_93'] = x_get_current_user__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_94'] = x_get_current_user__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_95'] = x_get_current_user__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_96'] = x_get_current_user__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_97'] = x_get_current_user__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_98'] = x_get_current_user__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_99'] = x_get_current_user__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_100'] = x_get_current_user__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_101'] = x_get_current_user__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_102'] = x_get_current_user__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_103'] = x_get_current_user__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_104'] = x_get_current_user__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_current_user__mutmut['x_get_current_user__mutmut_105'] = x_get_current_user__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_optional_user__mutmut)
async def get_optional_user(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, api_key_info, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_orig(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, api_key_info, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_1(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(None, api_key_info, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_2(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, None, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_3(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, api_key_info, None)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_4(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(api_key_info, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_5(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, bearer_payload)
    except HTTPException:
        return None


async def x_get_optional_user__mutmut_6(
    request: Request,
    api_key_info: dict[str, Any] | None = None,
    bearer_payload: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Obtiene el usuario autenticado de forma opcional (no lanza error si no hay credenciales).

    Args:
        request: Solicitud HTTP
        api_key_info: Datos de la API key (opcional)
        bearer_payload: Payload del Bearer token (opcional)

    Returns:
        Dict con datos del usuario o None si no hay credenciales
    """
    try:
        return await get_current_user(request, api_key_info, )
    except HTTPException:
        return None

mutants_x_get_optional_user__mutmut['_mutmut_orig'] = x_get_optional_user__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_1'] = x_get_optional_user__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_2'] = x_get_optional_user__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_3'] = x_get_optional_user__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_4'] = x_get_optional_user__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_5'] = x_get_optional_user__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_optional_user__mutmut['x_get_optional_user__mutmut_6'] = x_get_optional_user__mutmut_6 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_require_permission__mutmut)
def require_permission(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_orig(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_1(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = None
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_2(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get(None, 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_3(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", None)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_4(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get(0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_5(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", )
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_6(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("XXuser_idXX", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_7(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("USER_ID", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_8(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 1)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_9(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = None
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_10(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get(None, "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_11(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", None)
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_12(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_13(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", )
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_14(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("XXroleXX", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_15(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("ROLE", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_16(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "XXviewerXX")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_17(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "VIEWER")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_18(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = None

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_19(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(None)

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_20(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) and [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_21(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get(None, []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_22(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", None) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_23(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get([]) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_24(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", ) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_25(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("XXscopesXX", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_26(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("SCOPES", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_27(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(None, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_28(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, None, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_29(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, None, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_30(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, None):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_31(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_32(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_33(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_34(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, ):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_35(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = None
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_36(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(None, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_37(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, None, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_38(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, None):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_39(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_40(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_41(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, ):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_42(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=None,
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_43(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=None,
        )

    return _check_permission


def x_require_permission__mutmut_44(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            detail=f"Permiso denegado: {resource}:{action}. "
            f"Contacte al administrador para obtener acceso.",
        )

    return _check_permission


def x_require_permission__mutmut_45(resource: str, action: str):
    """Crea una dependencia que verifica permisos RBAC granulares.

    Fix Sprint 5 BUG-ARCH-03: usa `has_permission` del módulo compartido
    `security.auth_shared` para evitar drift con Flask.

    Args:
        resource: Recurso a verificar (workflow, connector, tool, etc.)
        action: Accion a verificar (create, read, update, delete, execute, etc.)

    Returns:
        Dependencia FastAPI que verifica el permiso

    Usage:
        @router.post("/", dependencies=[Depends(require_permission("workflow", "create"))])
    """
    from src.core.security.auth_shared import has_permission

    async def _check_permission(user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any]:
        """Verifica si el usuario tiene el permiso especificado.

        Args:
            user: Usuario autenticado (inyectado por get_current_user)

        Returns:
            Datos del usuario si tiene permiso

        Raises:
            HTTPException: Si el usuario no tiene el permiso requerido
        """
        user_id = user.get("user_id", 0)
        user_role = user.get("role", "viewer")
        scopes = set(user.get("scopes", []) or [])

        # Usar función compartida has_permission (fix BUG-ARCH-03)
        if has_permission(user_role, scopes, resource, action):
            return user

        # Fallback a RBACManager para permisos granulares en DB
        rbac = RBACManager()
        if rbac.check_permission(user_id, resource, action):
            return user

        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            )

    return _check_permission

mutants_x_require_permission__mutmut['_mutmut_orig'] = x_require_permission__mutmut_orig # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_1'] = x_require_permission__mutmut_1 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_2'] = x_require_permission__mutmut_2 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_3'] = x_require_permission__mutmut_3 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_4'] = x_require_permission__mutmut_4 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_5'] = x_require_permission__mutmut_5 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_6'] = x_require_permission__mutmut_6 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_7'] = x_require_permission__mutmut_7 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_8'] = x_require_permission__mutmut_8 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_9'] = x_require_permission__mutmut_9 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_10'] = x_require_permission__mutmut_10 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_11'] = x_require_permission__mutmut_11 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_12'] = x_require_permission__mutmut_12 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_13'] = x_require_permission__mutmut_13 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_14'] = x_require_permission__mutmut_14 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_15'] = x_require_permission__mutmut_15 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_16'] = x_require_permission__mutmut_16 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_17'] = x_require_permission__mutmut_17 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_18'] = x_require_permission__mutmut_18 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_19'] = x_require_permission__mutmut_19 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_20'] = x_require_permission__mutmut_20 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_21'] = x_require_permission__mutmut_21 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_22'] = x_require_permission__mutmut_22 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_23'] = x_require_permission__mutmut_23 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_24'] = x_require_permission__mutmut_24 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_25'] = x_require_permission__mutmut_25 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_26'] = x_require_permission__mutmut_26 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_27'] = x_require_permission__mutmut_27 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_28'] = x_require_permission__mutmut_28 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_29'] = x_require_permission__mutmut_29 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_30'] = x_require_permission__mutmut_30 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_31'] = x_require_permission__mutmut_31 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_32'] = x_require_permission__mutmut_32 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_33'] = x_require_permission__mutmut_33 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_34'] = x_require_permission__mutmut_34 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_35'] = x_require_permission__mutmut_35 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_36'] = x_require_permission__mutmut_36 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_37'] = x_require_permission__mutmut_37 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_38'] = x_require_permission__mutmut_38 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_39'] = x_require_permission__mutmut_39 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_40'] = x_require_permission__mutmut_40 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_41'] = x_require_permission__mutmut_41 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_42'] = x_require_permission__mutmut_42 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_43'] = x_require_permission__mutmut_43 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_44'] = x_require_permission__mutmut_44 # type: ignore # mutmut generated
mutants_x_require_permission__mutmut['x_require_permission__mutmut_45'] = x_require_permission__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_tenant__mutmut)
async def get_tenant(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_orig(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_1(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = None

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_2(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = None
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_3(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get(None, "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_4(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", None)
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_5(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_6(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", )
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_7(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("XXX-Tenant-IDXX", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_8(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("x-tenant-id", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_9(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-TENANT-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_10(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "XXXX")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_11(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = None
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_12(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(None)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_13(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant or tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_14(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get(None) == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_15(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("XXstatusXX") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_16(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("STATUS") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_17(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") != "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_18(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "XXactiveXX":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_19(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "ACTIVE":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_20(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=None,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_21(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=None,
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_22(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_23(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_24(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = None
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_25(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get(None)
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_26(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("XXtenant_idXX")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_27(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("TENANT_ID")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_28(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = None
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_29(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(None)
        if tenant and tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_30(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant or tenant.get("status") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_31(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get(None) == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_32(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("XXstatusXX") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_33(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("STATUS") == "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_34(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") != "active":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_35(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "XXactiveXX":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None


async def x_get_tenant__mutmut_36(request: Request, user: dict[str, Any] = Depends(get_current_user)) -> dict[str, Any] | None:
    """Resuelve el tenant desde la solicitud y el usuario autenticado.

    Orden de resolucion:
    1. Header X-Tenant-ID (para API directa)
    2. Tenant ID del usuario autenticado (si viene de API key)
    3. Subdominio (tenant.zenic-flijo.com)
    4. Ningun tenant (modo sin multi-tenancy)

    Args:
        request: Solicitud HTTP
        user: Usuario autenticado

    Returns:
        Dict con datos del tenant o None

    Raises:
        HTTPException: Si el tenant especificado no existe o esta inactivo
    """
    tenant_service = TenantService()

    # 1. Header X-Tenant-ID
    tenant_id = request.headers.get("X-Tenant-ID", "")
    if tenant_id:
        tenant = tenant_service.get_tenant(tenant_id)
        if tenant and tenant.get("status") == "active":
            return tenant
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Tenant '{tenant_id}' no encontrado o inactivo.",
        )

    # 2. Tenant ID del usuario
    user_tenant_id = user.get("tenant_id")
    if user_tenant_id:
        tenant = tenant_service.get_tenant(user_tenant_id)
        if tenant and tenant.get("status") == "ACTIVE":
            return tenant

    # 3. Sin tenant (modo sin multi-tenancy)
    return None

mutants_x_get_tenant__mutmut['_mutmut_orig'] = x_get_tenant__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_1'] = x_get_tenant__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_2'] = x_get_tenant__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_3'] = x_get_tenant__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_4'] = x_get_tenant__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_5'] = x_get_tenant__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_6'] = x_get_tenant__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_7'] = x_get_tenant__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_8'] = x_get_tenant__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_9'] = x_get_tenant__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_10'] = x_get_tenant__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_11'] = x_get_tenant__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_12'] = x_get_tenant__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_13'] = x_get_tenant__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_14'] = x_get_tenant__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_15'] = x_get_tenant__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_16'] = x_get_tenant__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_17'] = x_get_tenant__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_18'] = x_get_tenant__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_19'] = x_get_tenant__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_20'] = x_get_tenant__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_21'] = x_get_tenant__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_22'] = x_get_tenant__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_23'] = x_get_tenant__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_24'] = x_get_tenant__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_25'] = x_get_tenant__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_26'] = x_get_tenant__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_27'] = x_get_tenant__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_28'] = x_get_tenant__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_29'] = x_get_tenant__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_30'] = x_get_tenant__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_31'] = x_get_tenant__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_32'] = x_get_tenant__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_33'] = x_get_tenant__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_34'] = x_get_tenant__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_35'] = x_get_tenant__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_tenant__mutmut['x_get_tenant__mutmut_36'] = x_get_tenant__mutmut_36 # type: ignore # mutmut generated
