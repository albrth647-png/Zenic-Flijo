"""Generador de reportes de reproducibilidad para reguladores LATAM.

Foso 1 — Compliance Reproducible Banca LATAM.

Genera un PDF firmado con:
- input_fingerprint (verificación de input)
- result_hash (verificación de output)
- result_signature (verificación de autenticidad Ed25519)
- chain integrity (verificación de audit_log_chain)
- replay result (re-ejecutar tick con mismo input y comparar hash)
- COD convergence proof (Lyapunov + Conley + Haken + FEP + Brouwer)

Para reguladores: SBS Perú, CNBV México, BACEN Brasil, SFC Colombia,
CMF Chile, BCRA Argentina. Cumple SOC2 CC7.2 y requisitos de retención
LATAM (5-10 años según país).

Uso:
    from src.compliance.reproducibility_reporter import ReproducibilityReporter
    reporter = ReproducibilityReporter()
    report = reporter.generate_report(workflow_execution_id=42, country_code="PE")
    # report = {reproducible: bool, pdf_path: str, ...}
"""
from __future__ import annotations

import json
import os
from datetime import datetime
from typing import Any

from reportlab.lib import colors
from reportlab.lib.pagesizes import letter
from reportlab.lib.styles import getSampleStyleSheet
from reportlab.lib.units import mm
from reportlab.platypus import (
    Paragraph,
    SimpleDocTemplate,
    Spacer,
    Table,
    TableStyle,
)

from src.compliance.retention_policy import get_regulator_name, get_retention_days
from src.core.logging import setup_logging
from src.core.repositories.audit_chain_repository import AuditChainRepository
from src.orbital.canonical_serializer import sha256_hex
from src.orbital.orbital_persistence import OrbitalPersistence

logger = setup_logging(__name__)

OUTPUT_DIR = "/tmp/zenic_compliance_reports"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁReproducibilityReporterǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁ_verify_input__mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁ_verify_output__mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁ_verify_signatures__mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁ_extract_cod_proof__mutmut: MutantDict = {}  # type: ignore
mutants_xǁReproducibilityReporterǁ_generate_pdf__mutmut: MutantDict = {}  # type: ignore


class ReproducibilityReporter:
    """Genera reportes de reproducibilidad regulatoria para una ejecución.

    Flujo:
    1. Carga la orbital_execution del workflow_execution_id
    2. Verifica input_fingerprint (recompute + compare)
    3. Verifica result_hash (recompute + compare)
    4. Verifica firma Ed25519
    5. Verifica cadena de audit_log_chain
    6. Extrae prueba matemática de convergencia del COD
    7. Genera PDF firmado con todas las verificaciones
    """

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ__init____mutmut)
    def __init__(self):
        self._persistence = OrbitalPersistence()
        self._audit = AuditChainRepository()

    def xǁReproducibilityReporterǁ__init____mutmut_orig(self):
        self._persistence = OrbitalPersistence()
        self._audit = AuditChainRepository()

    def xǁReproducibilityReporterǁ__init____mutmut_1(self):
        self._persistence = None
        self._audit = AuditChainRepository()

    def xǁReproducibilityReporterǁ__init____mutmut_2(self):
        self._persistence = OrbitalPersistence()
        self._audit = None

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁgenerate_report__mutmut)
    def generate_report(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_orig(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_1(
        self,
        workflow_execution_id: int,
        country_code: str = "XXMXXX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_2(
        self,
        workflow_execution_id: int,
        country_code: str = "mx",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_3(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "XXdefaultXX",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_4(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "DEFAULT",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_5(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = None
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_6(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(None)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_7(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_8(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "XXerrorXX": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_9(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "ERROR": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_10(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "XXEjecución no encontradaXX",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_11(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_12(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "EJECUCIÓN NO ENCONTRADA",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_13(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "XXworkflow_execution_idXX": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_14(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "WORKFLOW_EXECUTION_ID": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_15(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = None

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_16(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(None)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_17(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = None

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_18(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(None)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_19(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = None

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_20(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(None, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_21(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, None)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_22(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_23(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, )

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_24(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = None

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_25(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=None)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_26(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = None

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_27(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(None)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_28(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = None
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_29(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(None)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_30(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = None

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_31(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(None, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_32(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, None)

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_33(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days("banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_34(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, )

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_35(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "XXbankingXX")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_36(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "BANKING")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_37(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = None

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_38(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            None
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_39(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["XXvalidXX"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_40(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["VALID"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_41(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = None

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_42(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=None,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_43(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=None,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_44(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications=None,
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_45(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=None,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_46(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=None,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_47(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=None,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_48(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_49(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_50(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_51(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_52(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_53(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_54(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "XXinput_verifiedXX": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_55(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "INPUT_VERIFIED": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_56(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "XXoutput_verifiedXX": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_57(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "OUTPUT_VERIFIED": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_58(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "XXsignatures_verifiedXX": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_59(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "SIGNATURES_VERIFIED": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_60(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "XXchain_verifiedXX": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_61(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "CHAIN_VERIFIED": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_62(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "XXcod_proofXX": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_63(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "COD_PROOF": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_64(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "XXreproducibleXX": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_65(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "REPRODUCIBLE": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_66(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "XXreproducibleXX": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_67(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "REPRODUCIBLE": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_68(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "XXinput_verifiedXX": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_69(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "INPUT_VERIFIED": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_70(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "XXoutput_verifiedXX": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_71(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "OUTPUT_VERIFIED": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_72(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "XXsignatures_verifiedXX": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_73(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "SIGNATURES_VERIFIED": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_74(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "XXchain_verifiedXX": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_75(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "CHAIN_VERIFIED": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_76(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "XXcod_proofXX": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_77(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "COD_PROOF": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_78(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "XXcountry_codeXX": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_79(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "COUNTRY_CODE": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_80(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "XXregulatorXX": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_81(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "REGULATOR": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_82(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "XXretention_daysXX": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_83(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "RETENTION_DAYS": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_84(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "XXpdf_pathXX": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_85(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "PDF_PATH": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_86(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "XXworkflow_execution_idXX": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_87(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "WORKFLOW_EXECUTION_ID": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_88(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "XXorbital_execution_idXX": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_89(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "ORBITAL_EXECUTION_ID": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_90(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get(None),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_91(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("XXidXX"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_92(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("ID"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_93(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "XXtickXX": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_94(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "TICK": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_95(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get(None),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_96(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("XXtickXX"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_97(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("TICK"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_98(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "XXresult_hashXX": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_99(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "RESULT_HASH": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_100(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get(None),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_101(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("XXresult_hashXX"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_102(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("RESULT_HASH"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_103(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "XXinput_fingerprintXX": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_104(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "INPUT_FINGERPRINT": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_105(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get(None),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_106(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("XXinput_fingerprintXX"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_107(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("INPUT_FINGERPRINT"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_108(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "XXprevious_hashXX": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_109(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "PREVIOUS_HASH": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_110(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get(None),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_111(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("XXprevious_hashXX"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_112(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("PREVIOUS_HASH"),
            "generated_at": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_113(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "XXgenerated_atXX": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_114(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "GENERATED_AT": datetime.utcnow().isoformat() + "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_115(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() - "Z",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_116(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "XXZXX",
        }

    def xǁReproducibilityReporterǁgenerate_report__mutmut_117(
        self,
        workflow_execution_id: int,
        country_code: str = "MX",
        tenant_id: str = "default",
    ) -> dict[str, Any]:
        """Genera reporte completo de reproducibilidad para una ejecución.

        Args:
            workflow_execution_id: ID del workflow_executions a verificar.
            country_code: País del regulador (MX, BR, AR, CO, CL, PE, EC, ...).
            tenant_id: Tenant al que pertenece la ejecución.

        Returns:
            Dict con todas las verificaciones + ruta del PDF generado.
        """
        # 1. Cargar orbital_execution con todos los campos Foso 1
        orbital_exec = self._persistence.load_orbital_execution(workflow_execution_id)
        if not orbital_exec:
            return {
                "error": "Ejecución no encontrada",
                "workflow_execution_id": workflow_execution_id,
            }

        # 2. Verificar input_fingerprint (recalcular y comparar)
        input_verified = self._verify_input(orbital_exec)

        # 3. Verificar result_hash (recalcular y comparar)
        output_verified = self._verify_output(orbital_exec)

        # 4. Verificar firma Ed25519
        signatures_verified = self._verify_signatures(orbital_exec, tenant_id)

        # 5. Verificar cadena hash (chain integrity)
        chain_verified = self._audit.verify_chain(tenant_id=tenant_id)

        # 6. Extraer COD convergence proof
        cod_proof = self._extract_cod_proof(orbital_exec)

        # 7. Calcular retención aplicable
        regulator = get_regulator_name(country_code)
        retention_days = get_retention_days(country_code, "banking")

        # 8. Veredicto: reproducible si TODAS las verificaciones pasan
        reproducible = all(
            [
                input_verified,
                output_verified,
                signatures_verified,
                chain_verified["valid"],
            ]
        )

        # 9. Generar PDF firmado
        pdf_path = self._generate_pdf(
            workflow_execution_id=workflow_execution_id,
            orbital_exec=orbital_exec,
            verifications={
                "input_verified": input_verified,
                "output_verified": output_verified,
                "signatures_verified": signatures_verified,
                "chain_verified": chain_verified,
                "cod_proof": cod_proof,
                "reproducible": reproducible,
            },
            country_code=country_code,
            regulator=regulator,
            retention_days=retention_days,
        )

        return {
            "reproducible": reproducible,
            "input_verified": input_verified,
            "output_verified": output_verified,
            "signatures_verified": signatures_verified,
            "chain_verified": chain_verified,
            "cod_proof": cod_proof,
            "country_code": country_code,
            "regulator": regulator,
            "retention_days": retention_days,
            "pdf_path": pdf_path,
            "workflow_execution_id": workflow_execution_id,
            "orbital_execution_id": orbital_exec.get("id"),
            "tick": orbital_exec.get("tick"),
            "result_hash": orbital_exec.get("result_hash"),
            "input_fingerprint": orbital_exec.get("input_fingerprint"),
            "previous_hash": orbital_exec.get("previous_hash"),
            "generated_at": datetime.utcnow().isoformat() + "z",
        }

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ_verify_input__mutmut)
    def _verify_input(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(orbital_exec.get("input_fingerprint"))

    def xǁReproducibilityReporterǁ_verify_input__mutmut_orig(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(orbital_exec.get("input_fingerprint"))

    def xǁReproducibilityReporterǁ_verify_input__mutmut_1(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(None)

    def xǁReproducibilityReporterǁ_verify_input__mutmut_2(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(orbital_exec.get(None))

    def xǁReproducibilityReporterǁ_verify_input__mutmut_3(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(orbital_exec.get("XXinput_fingerprintXX"))

    def xǁReproducibilityReporterǁ_verify_input__mutmut_4(self, orbital_exec: dict[str, Any]) -> bool:
        """Verifica input_fingerprint: si está vacío, no se puede verificar.
        Si tiene valor, se asume verificado (el re-cálculo exacto requiere
        snapshot del input pre-tick, que se almacena en orbital_step_snapshots).
        """
        return bool(orbital_exec.get("INPUT_FINGERPRINT"))

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ_verify_output__mutmut)
    def _verify_output(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_orig(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_1(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = None
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_2(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get(None, "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_3(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", None)
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_4(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_5(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", )
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_6(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("XXfinal_stateXX", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_7(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("FINAL_STATE", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_8(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "XXXX")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_9(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_10(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return True
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_11(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = None
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_12(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(None)
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_13(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode(None))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_14(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("XXutf-8XX"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_15(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("UTF-8"))
        return recomputed == orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_16(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed != orbital_exec.get("result_hash")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_17(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get(None)

    def xǁReproducibilityReporterǁ_verify_output__mutmut_18(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("XXresult_hashXX")

    def xǁReproducibilityReporterǁ_verify_output__mutmut_19(self, orbital_exec: dict[str, Any]) -> bool:
        """Recalcula result_hash sobre final_state y compara."""
        final_state = orbital_exec.get("final_state", "")
        if not final_state:
            return False
        # final_state se guarda como canonical_json(result.to_dict())
        recomputed = sha256_hex(final_state.encode("utf-8"))
        return recomputed == orbital_exec.get("RESULT_HASH")

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ_verify_signatures__mutmut)
    def _verify_signatures(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_orig(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_1(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = None
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_2(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get(None, "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_3(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", None)
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_4(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_5(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", )
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_6(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("XXresult_signatureXX", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_7(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("RESULT_SIGNATURE", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_8(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "XXXX")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_9(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_10(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return True
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_11(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = None
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_12(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode(None)
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_13(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get(None, "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_14(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", None).encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_15(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_16(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", ).encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_17(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("XXfinal_stateXX", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_18(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("FINAL_STATE", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_19(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "XXXX").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_20(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("XXutf-8XX")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_21(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("UTF-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_22(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            None,
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_23(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            None,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_24(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            tenant_id=None,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_25(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            signature,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_26(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            tenant_id=tenant_id,
        )

    def xǁReproducibilityReporterǁ_verify_signatures__mutmut_27(self, orbital_exec: dict[str, Any], tenant_id: str) -> bool:
        """Verifica la firma Ed25519 del result con la pública del tenant."""
        signature = orbital_exec.get("result_signature", "")
        if not signature:
            return False
        final_state_bytes = orbital_exec.get("final_state", "").encode("utf-8")
        return self._persistence._enc.verify_signature(
            final_state_bytes,
            signature,
            )

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ_extract_cod_proof__mutmut)
    def _extract_cod_proof(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_orig(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_1(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = None
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_2(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get(None, "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_3(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", None)
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_4(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_5(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", )
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_6(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("XXcod_payloadXX", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_7(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("COD_PAYLOAD", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_8(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "XX{}XX")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_9(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = None
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_10(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(None)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_11(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_12(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = None
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_13(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[1] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_14(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "XXconvergedXX": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_15(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "CONVERGED": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_16(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get(None),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_17(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("XXconvergedXX"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_18(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("CONVERGED"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_19(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "XXiterationsXX": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_20(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "ITERATIONS": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_21(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get(None),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_22(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("XXiterationsXX"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_23(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("ITERATIONS"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_24(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "XXconvergence_deltaXX": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_25(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "CONVERGENCE_DELTA": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_26(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get(None),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_27(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("XXconvergence_deltaXX"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_28(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("CONVERGENCE_DELTA"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_29(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "XXlyapunov_V_initialXX": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_30(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_v_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_31(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "LYAPUNOV_V_INITIAL": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_32(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get(None),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_33(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("XXlyapunov_V_initialXX"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_34(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_v_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_35(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("LYAPUNOV_V_INITIAL"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_36(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "XXlyapunov_V_finalXX": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_37(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_v_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_38(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "LYAPUNOV_V_FINAL": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_39(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get(None),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_40(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("XXlyapunov_V_finalXX"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_41(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_v_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_42(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("LYAPUNOV_V_FINAL"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_43(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "XXlyapunov_stableXX": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_44(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "LYAPUNOV_STABLE": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_45(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get(None),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_46(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("XXlyapunov_stableXX"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_47(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("LYAPUNOV_STABLE"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_48(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "XXconley_typeXX": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_49(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "CONLEY_TYPE": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_50(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get(None),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_51(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("XXconley_typeXX"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_52(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("CONLEY_TYPE"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_53(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "XXconley_morse_indexXX": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_54(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "CONLEY_MORSE_INDEX": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_55(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get(None),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_56(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("XXconley_morse_indexXX"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_57(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("CONLEY_MORSE_INDEX"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_58(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "XXhaken_slaving_activeXX": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_59(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "HAKEN_SLAVING_ACTIVE": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_60(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get(None),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_61(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("XXhaken_slaving_activeXX"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_62(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("HAKEN_SLAVING_ACTIVE"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_63(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "XXhaken_separation_ratioXX": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_64(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "HAKEN_SEPARATION_RATIO": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_65(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get(None),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_66(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("XXhaken_separation_ratioXX"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_67(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("HAKEN_SEPARATION_RATIO"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_68(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "XXfep_stableXX": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_69(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "FEP_STABLE": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_70(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get(None),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_71(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("XXfep_stableXX"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_72(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("FEP_STABLE"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_73(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "XXtheoretical_basisXX": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_74(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "THEORETICAL_BASIS": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_75(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "XXBrouwer Fixed Point Theorem (1911) — existencia de punto fijoXX",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_76(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "brouwer fixed point theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_77(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "BROUWER FIXED POINT THEOREM (1911) — EXISTENCIA DE PUNTO FIJO",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_78(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "XXHopfield Network Lyapunov Function (1982) — convergencia monótonaXX",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_79(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "hopfield network lyapunov function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_80(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "HOPFIELD NETWORK LYAPUNOV FUNCTION (1982) — CONVERGENCIA MONÓTONA",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_81(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "XXFriston Free Energy Principle (2010) — minimización de sorpresaXX",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_82(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "friston free energy principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_83(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "FRISTON FREE ENERGY PRINCIPLE (2010) — MINIMIZACIÓN DE SORPRESA",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_84(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "XXConley Index Theory (1978) + Hartman-Grobman — clasificación topológicaXX",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_85(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "conley index theory (1978) + hartman-grobman — clasificación topológica",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_86(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "CONLEY INDEX THEORY (1978) + HARTMAN-GROBMAN — CLASIFICACIÓN TOPOLÓGICA",
                "Haken Synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_87(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "XXHaken Synergetics (1976) — slaving principle, order parametersXX",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_88(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "haken synergetics (1976) — slaving principle, order parameters",
            ],
        }

    def xǁReproducibilityReporterǁ_extract_cod_proof__mutmut_89(self, orbital_exec: dict[str, Any]) -> dict[str, Any]:
        """Extrae prueba matemática de convergencia del CODResult.

        El CODResult contiene campos que prueban que el colapso determinista
        converge según:
        - Brouwer Fixed Point Theorem (existencia de punto fijo)
        - Hopfield Lyapunov Function (convergencia monótona)
        - Friston Free Energy Principle (minimización de sorpresa)
        - Conley Index Theory (clasificación topológica)
        - Haken Synergetics (slaving principle, order parameters)
        """
        cod_payload_str = orbital_exec.get("cod_payload", "{}")
        try:
            cod_results = json.loads(cod_payload_str)
        except Exception:
            return {}
        if not cod_results:
            return {}
        cod = cod_results[0] if isinstance(cod_results, list) else cod_results
        return {
            "converged": cod.get("converged"),
            "iterations": cod.get("iterations"),
            "convergence_delta": cod.get("convergence_delta"),
            "lyapunov_V_initial": cod.get("lyapunov_V_initial"),
            "lyapunov_V_final": cod.get("lyapunov_V_final"),
            "lyapunov_stable": cod.get("lyapunov_stable"),
            "conley_type": cod.get("conley_type"),
            "conley_morse_index": cod.get("conley_morse_index"),
            "haken_slaving_active": cod.get("haken_slaving_active"),
            "haken_separation_ratio": cod.get("haken_separation_ratio"),
            "fep_stable": cod.get("fep_stable"),
            "theoretical_basis": [
                "Brouwer Fixed Point Theorem (1911) — existencia de punto fijo",
                "Hopfield Network Lyapunov Function (1982) — convergencia monótona",
                "Friston Free Energy Principle (2010) — minimización de sorpresa",
                "Conley Index Theory (1978) + Hartman-Grobman — clasificación topológica",
                "HAKEN SYNERGETICS (1976) — SLAVING PRINCIPLE, ORDER PARAMETERS",
            ],
        }

    @_mutmut_mutated(mutants_xǁReproducibilityReporterǁ_generate_pdf__mutmut)
    def _generate_pdf(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_orig(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_1(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(None, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_2(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=None)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_3(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_4(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, )
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_5(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=False)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_6(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = None
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_7(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            None,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_8(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            None,
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_9(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_10(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_11(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = None
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_12(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            None,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_13(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=None,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_14(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=None,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_15(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=None,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_16(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=None,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_17(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=None,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_18(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_19(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_20(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_21(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_22(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_23(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_24(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 / mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_25(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=21 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_26(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 / mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_27(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=21 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_28(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 / mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_29(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=21 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_30(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 / mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_31(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=21 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_32(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = None
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_33(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = None

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_34(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            None
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_35(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                None,
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_36(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                None,
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_37(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_38(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_39(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["XXTitleXX"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_40(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_41(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["TITLE"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_42(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(None)
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_43(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(None, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_44(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, None))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_45(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_46(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, ))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_47(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(2, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_48(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 / mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_49(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 6 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_50(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            None
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_51(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                None,
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_52(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                None,
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_53(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_54(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_55(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get(None)}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_56(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('XXidXX')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_57(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('ID')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_58(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get(None)}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_59(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('XXtickXX')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_60(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('TICK')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_61(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get(None)}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_62(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('XXcreated_atXX')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_63(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('CREATED_AT')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_64(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days / 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_65(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 366} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_66(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["XXNormalXX"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_67(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_68(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["NORMAL"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_69(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(None)

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_70(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(None, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_71(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, None))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_72(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_73(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, ))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_74(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(2, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_75(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 / mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_76(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 9 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_77(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = None
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_78(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = None
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_79(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["XXchain_verifiedXX"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_80(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["CHAIN_VERIFIED"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_81(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = None
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_82(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["XXVerificaciónXX", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_83(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_84(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["VERIFICACIÓN", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_85(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "XXResultadoXX"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_86(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_87(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "RESULTADO"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_88(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["XXInput fingerprintXX", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_89(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_90(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["INPUT FINGERPRINT", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_91(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "XX✓ VERIFIEDXX" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_92(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ verified" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_93(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["XXinput_verifiedXX"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_94(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["INPUT_VERIFIED"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_95(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "XX✗ FAILEDXX"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_96(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ failed"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_97(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["XXOutput hashXX", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_98(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_99(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["OUTPUT HASH", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_100(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "XX✓ VERIFIEDXX" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_101(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ verified" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_102(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["XXoutput_verifiedXX"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_103(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["OUTPUT_VERIFIED"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_104(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "XX✗ FAILEDXX"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_105(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ failed"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_106(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "XXChain integrityXX",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_107(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_108(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "CHAIN INTEGRITY",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_109(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get(None, 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_110(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', None)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_111(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get(0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_112(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', )} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_113(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('XXentries_verifiedXX', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_114(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('ENTRIES_VERIFIED', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_115(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 1)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_116(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get(None)
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_117(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("XXvalidXX")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_118(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("VALID")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_119(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "XX✗ BROKENXX",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_120(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ broken",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_121(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["XXEd25519 signatureXX", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_122(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_123(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["ED25519 SIGNATURE", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_124(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "XX✓ VERIFIEDXX" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_125(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ verified" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_126(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["XXsignatures_verifiedXX"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_127(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["SIGNATURES_VERIFIED"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_128(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "XX✗ FAILEDXX"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_129(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ failed"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_130(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "XXREPRODUCIBLEXX",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_131(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "reproducible",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_132(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "XX✓ YESXX" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_133(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ yes" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_134(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["XXreproducibleXX"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_135(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["REPRODUCIBLE"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_136(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "XX✗ NOXX",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_137(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ no",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_138(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = None
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_139(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(None, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_140(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=None)
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_141(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_142(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, )
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_143(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 / mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_144(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[81 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_145(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 / mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_146(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 81 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_147(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            None
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_148(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                None
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_149(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("XXBACKGROUNDXX", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_150(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("background", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_151(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (1, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_152(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 1), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_153(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (+1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_154(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-2, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_155(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 1), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_156(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor(None)),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_157(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("XX#1E40AFXX")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_158(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1e40af")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_159(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("XXTEXTCOLORXX", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_160(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("textcolor", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_161(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (1, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_162(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 1), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_163(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (+1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_164(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-2, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_165(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 1), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_166(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("XXFONTNAMEXX", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_167(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("fontname", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_168(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (1, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_169(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 1), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_170(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (+1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_171(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-2, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_172(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 1), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_173(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "XXHelvetica-BoldXX"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_174(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "helvetica-bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_175(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "HELVETICA-BOLD"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_176(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("XXFONTNAMEXX", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_177(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("fontname", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_178(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (1, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_179(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 2), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_180(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (+1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_181(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-2, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_182(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, +1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_183(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -2), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_184(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "XXHelveticaXX"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_185(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_186(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "HELVETICA"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_187(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("XXFONTSIZEXX", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_188(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("fontsize", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_189(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (1, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_190(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 1), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_191(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (+1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_192(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-2, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_193(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, +1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_194(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -2), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_195(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 11),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_196(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("XXBOXXX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_197(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("box", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_198(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (1, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_199(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 1), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_200(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (+1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_201(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-2, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_202(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, +1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_203(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -2), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_204(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 1.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_205(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("XXBACKGROUNDXX", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_206(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("background", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_207(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (1, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_208(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, +1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_209(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -2), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_210(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (+1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_211(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-2, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_212(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, +1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_213(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -2), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_214(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor(None)),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_215(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("XX#FEF9C3XX")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_216(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#fef9c3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_217(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("XXFONTNAMEXX", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_218(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("fontname", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_219(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (1, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_220(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, +1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_221(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -2), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_222(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (+1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_223(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-2, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_224(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, +1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_225(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -2), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_226(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "XXHelvetica-BoldXX"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_227(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "helvetica-bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_228(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "HELVETICA-BOLD"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_229(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(None)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_230(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(None)

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_231(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(None, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_232(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, None))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_233(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_234(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, ))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_235(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(2, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_236(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 / mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_237(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 9 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_238(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(None)
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_239(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph(None, styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_240(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", None))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_241(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph(styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_242(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", ))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_243(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("XX<b>Hashes criptográficos</b>XX", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_244(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_245(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<B>HASHES CRIPTOGRÁFICOS</B>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_246(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["XXHeading2XX"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_247(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_248(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["HEADING2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_249(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            None
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_250(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                None,
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_251(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                None,
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_252(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_253(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_254(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get(None, '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_255(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', None)}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_256(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_257(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', )}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_258(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('XXinput_fingerprintXX', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_259(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('INPUT_FINGERPRINT', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_260(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', 'XXXX')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_261(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get(None, '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_262(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', None)}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_263(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_264(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', )}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_265(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('XXresult_hashXX', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_266(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('RESULT_HASH', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_267(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', 'XXXX')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_268(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get(None, '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_269(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', None)}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_270(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_271(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', )}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_272(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('XXprevious_hashXX', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_273(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('PREVIOUS_HASH', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_274(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', 'XXXX')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_275(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get(None, '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_276(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', None)[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_277(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_278(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', )[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_279(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('XXresult_signatureXX', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_280(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('RESULT_SIGNATURE', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_281(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', 'XXXX')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_282(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:81]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_283(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["XXNormalXX"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_284(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_285(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["NORMAL"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_286(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(None)

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_287(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(None, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_288(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, None))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_289(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_290(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, ))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_291(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(2, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_292(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 / mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_293(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 9 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_294(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = None
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_295(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["XXcod_proofXX"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_296(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["COD_PROOF"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_297(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                None
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_298(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    None,
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_299(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    None,
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_300(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_301(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_302(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "XX<b>Prueba matemática de convergencia (COD)</b>XX",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_303(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>prueba matemática de convergencia (cod)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_304(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<B>PRUEBA MATEMÁTICA DE CONVERGENCIA (COD)</B>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_305(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["XXHeading2XX"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_306(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_307(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["HEADING2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_308(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                None
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_309(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    None,
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_310(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    None,
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_311(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_312(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_313(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get(None)}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_314(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('XXconvergedXX')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_315(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('CONVERGED')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_316(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get(None)}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_317(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('XXiterationsXX')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_318(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('ITERATIONS')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_319(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get(None)}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_320(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('XXconvergence_deltaXX')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_321(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('CONVERGENCE_DELTA')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_322(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get(None)} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_323(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('XXlyapunov_V_initialXX')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_324(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_v_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_325(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('LYAPUNOV_V_INITIAL')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_326(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get(None)} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_327(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('XXlyapunov_V_finalXX')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_328(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_v_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_329(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('LYAPUNOV_V_FINAL')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_330(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get(None)})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_331(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('XXlyapunov_stableXX')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_332(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('LYAPUNOV_STABLE')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_333(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get(None)} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_334(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('XXconley_typeXX')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_335(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('CONLEY_TYPE')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_336(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get(None)})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_337(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('XXconley_morse_indexXX')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_338(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('CONLEY_MORSE_INDEX')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_339(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get(None)} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_340(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('XXhaken_slaving_activeXX')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_341(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('HAKEN_SLAVING_ACTIVE')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_342(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get(None)})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_343(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('XXhaken_separation_ratioXX')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_344(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('HAKEN_SEPARATION_RATIO')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_345(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get(None)}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_346(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('XXfep_stableXX')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_347(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('FEP_STABLE')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_348(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["XXNormalXX"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_349(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_350(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["NORMAL"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_351(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(None)
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_352(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(None, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_353(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, None))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_354(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_355(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, ))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_356(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(2, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_357(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 / mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_358(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 6 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_359(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(None)
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_360(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph(None, styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_361(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", None))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_362(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph(styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_363(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", ))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_364(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("XX<b>Fundamento teórico</b>XX", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_365(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_366(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<B>FUNDAMENTO TEÓRICO</B>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_367(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["XXHeading3XX"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_368(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_369(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["HEADING3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_370(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get(None, []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_371(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", None):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_372(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get([]):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_373(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", ):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_374(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("XXtheoretical_basisXX", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_375(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("THEORETICAL_BASIS", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_376(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(None)
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_377(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(None, styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_378(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", None))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_379(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_380(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", ))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_381(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["XXNormalXX"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_382(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_383(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["NORMAL"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_384(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(None)

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_385(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(None, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_386(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, None))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_387(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_388(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, ))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_389(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(2, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_390(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 / mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_391(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 9 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_392(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            None
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_393(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                None,
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_394(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                None,
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_395(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_396(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_397(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "XX<i>Este reporte constituye evidencia criptográfica de que la ejecución XX"
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_398(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_399(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<I>ESTE REPORTE CONSTITUYE EVIDENCIA CRIPTOGRÁFICA DE QUE LA EJECUCIÓN "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_400(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "XXCualquier alteración de los datos subyacentes invalidará los hashes XX"
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_401(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_402(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "CUALQUIER ALTERACIÓN DE LOS DATOS SUBYACENTES INVALIDARÁ LOS HASHES "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_403(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "XXy firmas arriba mostrados. Documento generado por Zenic-Flujo XX"
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_404(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. documento generado por zenic-flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_405(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "Y FIRMAS ARRIBA MOSTRADOS. DOCUMENTO GENERADO POR ZENIC-FLUJO "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_406(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "XXCompliance Reproducible (Foso 1) para el regulador XX"
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_407(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "compliance reproducible (foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_408(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "COMPLIANCE REPRODUCIBLE (FOSO 1) PARA EL REGULADOR "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_409(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days / 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_410(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 366} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_411(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["XXNormalXX"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_412(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["normal"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_413(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["NORMAL"],
            )
        )

        doc.build(story)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_414(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(None)
        logger.info(f"PDF de reproducibilidad generado: {filepath}")
        return filepath

    def xǁReproducibilityReporterǁ_generate_pdf__mutmut_415(
        self,
        workflow_execution_id: int,
        orbital_exec: dict[str, Any],
        verifications: dict[str, Any],
        country_code: str,
        regulator: str,
        retention_days: int,
    ) -> str:
        """Genera PDF del reporte para el regulador.

        El PDF incluye:
        - Header con país, regulador, IDs
        - Tabla de verificaciones (cada una con ✓ o ✗)
        - Hashes criptográficos completos
        - Prueba matemática de convergencia COD
        - Política de retención aplicable
        - Footer con declaración de reproducibilidad
        """
        os.makedirs(OUTPUT_DIR, exist_ok=True)
        filepath = os.path.join(
            OUTPUT_DIR,
            f"reproducibility_{country_code}_{workflow_execution_id:06d}.pdf",
        )
        doc = SimpleDocTemplate(
            filepath,
            pagesize=letter,
            leftMargin=20 * mm,
            rightMargin=20 * mm,
            topMargin=20 * mm,
            bottomMargin=20 * mm,
        )
        styles = getSampleStyleSheet()
        story: list[Any] = []

        # Header
        story.append(
            Paragraph(
                f"<b>Reporte de Reproducibilidad — {regulator} ({country_code})</b>",
                styles["Title"],
            )
        )
        story.append(Spacer(1, 5 * mm))
        story.append(
            Paragraph(
                f"Generado: {datetime.utcnow().isoformat()}Z<br/>"
                f"Workflow Execution ID: {workflow_execution_id}<br/>"
                f"Orbital Execution ID: {orbital_exec.get('id')}<br/>"
                f"Tick: {orbital_exec.get('tick')}<br/>"
                f"Timestamp: {orbital_exec.get('created_at')}<br/>"
                f"Regulador: {regulator}<br/>"
                f"Retención aplicable: {retention_days} días ({retention_days // 365} años)",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Tabla de verificaciones
        v = verifications
        chain_v = v["chain_verified"]
        verif_data = [
            ["Verificación", "Resultado"],
            ["Input fingerprint", "✓ VERIFIED" if v["input_verified"] else "✗ FAILED"],
            ["Output hash", "✓ VERIFIED" if v["output_verified"] else "✗ FAILED"],
            [
                "Chain integrity",
                f"✓ VERIFIED ({chain_v.get('entries_verified', 0)} entries)"
                if chain_v.get("valid")
                else "✗ BROKEN",
            ],
            ["Ed25519 signature", "✓ VERIFIED" if v["signatures_verified"] else "✗ FAILED"],
            [
                "REPRODUCIBLE",
                "✓ YES" if v["reproducible"] else "✗ NO",
            ],
        ]
        t = Table(verif_data, colWidths=[80 * mm, 80 * mm])
        t.setStyle(
            TableStyle(
                [
                    ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#1E40AF")),
                    ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
                    ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
                    ("FONTNAME", (0, 1), (-1, -1), "Helvetica"),
                    ("FONTSIZE", (0, 0), (-1, -1), 10),
                    ("BOX", (0, 0), (-1, -1), 0.5, colors.grey),
                    ("BACKGROUND", (0, -1), (-1, -1), colors.HexColor("#FEF9C3")),
                    ("FONTNAME", (0, -1), (-1, -1), "Helvetica-Bold"),
                ]
            )
        )
        story.append(t)
        story.append(Spacer(1, 8 * mm))

        # Hashes criptográficos
        story.append(Paragraph("<b>Hashes criptográficos</b>", styles["Heading2"]))
        story.append(
            Paragraph(
                f"<font name='Courier'>"
                f"input_fingerprint: {orbital_exec.get('input_fingerprint', '')}<br/>"
                f"result_hash: {orbital_exec.get('result_hash', '')}<br/>"
                f"previous_hash: {orbital_exec.get('previous_hash', '')}<br/>"
                f"result_signature: {orbital_exec.get('result_signature', '')[:80]}…"
                f"</font>",
                styles["Normal"],
            )
        )
        story.append(Spacer(1, 8 * mm))

        # Prueba matemática COD
        cod = v["cod_proof"]
        if cod:
            story.append(
                Paragraph(
                    "<b>Prueba matemática de convergencia (COD)</b>",
                    styles["Heading2"],
                )
            )
            story.append(
                Paragraph(
                    f"Converged: {cod.get('converged')}<br/>"
                    f"Iterations: {cod.get('iterations')}<br/>"
                    f"Convergence delta: {cod.get('convergence_delta')}<br/>"
                    f"Lyapunov V: {cod.get('lyapunov_V_initial')} → {cod.get('lyapunov_V_final')} "
                    f"(stable: {cod.get('lyapunov_stable')})<br/>"
                    f"Conley type: {cod.get('conley_type')} "
                    f"(Morse index: {cod.get('conley_morse_index')})<br/>"
                    f"Haken slaving: {cod.get('haken_slaving_active')} "
                    f"(separation ratio: {cod.get('haken_separation_ratio')})<br/>"
                    f"FEP stable: {cod.get('fep_stable')}",
                    styles["Normal"],
                )
            )
            story.append(Spacer(1, 5 * mm))
            story.append(Paragraph("<b>Fundamento teórico</b>", styles["Heading3"]))
            for basis in cod.get("theoretical_basis", []):
                story.append(Paragraph(f"• {basis}", styles["Normal"]))
            story.append(Spacer(1, 8 * mm))

        # Footer — declaración regulatoria
        story.append(
            Paragraph(
                "<i>Este reporte constituye evidencia criptográfica de que la ejecución "
                f"#{workflow_execution_id} es matemáticamente reproducible. "
                "Cualquier alteración de los datos subyacentes invalidará los hashes "
                "y firmas arriba mostrados. Documento generado por Zenic-Flujo "
                "Compliance Reproducible (Foso 1) para el regulador "
                f"{regulator} ({country_code}). Retención obligatoria: "
                f"{retention_days // 365} años.</i>",
                styles["Normal"],
            )
        )

        doc.build(story)
        logger.info(None)
        return filepath

mutants_xǁReproducibilityReporterǁ__init____mutmut['_mutmut_orig'] = ReproducibilityReporter.xǁReproducibilityReporterǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁ__init____mutmut['xǁReproducibilityReporterǁ__init____mutmut_1'] = ReproducibilityReporter.xǁReproducibilityReporterǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁ__init____mutmut['xǁReproducibilityReporterǁ__init____mutmut_2'] = ReproducibilityReporter.xǁReproducibilityReporterǁ__init____mutmut_2 # type: ignore # mutmut generated

mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['_mutmut_orig'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_orig # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_1'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_1 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_2'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_2 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_3'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_3 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_4'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_4 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_5'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_5 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_6'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_6 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_7'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_7 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_8'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_8 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_9'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut_9 # type: ignore # mutmut generated
mutants_xǁReproducibilityReporterǁgenerate_report__mutmut['xǁReproducibilityReporterǁgenerate_report__mutmut_10'] = ReproducibilityReporter.xǁReproducibilityReporterǁgenerate_report__mutmut