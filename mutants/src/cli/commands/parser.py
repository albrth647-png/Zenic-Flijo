"""
Zenic CLI — Parser de argumentos
"""

from __future__ import annotations

import argparse

from src.cli.templates import VALID_AUTH_TYPES

CLI_VERSION = "1.0.0"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_build_parser__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_build_parser__mutmut)
def build_parser() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_orig() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_1() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = None

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_2() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog=None,
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_3() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description=None,
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_4() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog=None,
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_5() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_6() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_7() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_8() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="XXzenicXX",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_9() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="ZENIC",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_10() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="XXZenic CLI — Herramienta de desarrollo de conectores para Zenic-FlijoXX",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_11() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="zenic cli — herramienta de desarrollo de conectores para zenic-flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_12() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="ZENIC CLI — HERRAMIENTA DE DESARROLLO DE CONECTORES PARA ZENIC-FLIJO",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_13() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="XXUse 'zenic <comando> --help' para mas informacion sobre cada comando.XX",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_14() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_15() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="USE 'ZENIC <COMANDO> --HELP' PARA MAS INFORMACION SOBRE CADA COMANDO.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_16() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        None,
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_17() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action=None,
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_18() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=None,
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_19() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_20() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_21() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_22() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "XX--versionXX",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_23() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--VERSION",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_24() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="XXversionXX",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_25() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="VERSION",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_26() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = None

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_27() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title=None,
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_28() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest=None,
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_29() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help=None,
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_30() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_31() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_32() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_33() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="XXcomandosXX",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_34() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="COMANDOS",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_35() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="XXcommandXX",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_36() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="COMMAND",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_37() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="XXComando a ejecutarXX",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_38() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_39() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="COMANDO A EJECUTAR",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_40() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = None
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_41() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        None,
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_42() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help=None,
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_43() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description=None,
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_44() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_45() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_46() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_47() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "XXinitXX",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_48() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "INIT",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_49() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="XXCrea el scaffolding de un nuevo conectorXX",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_50() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_51() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="CREA EL SCAFFOLDING DE UN NUEVO CONECTOR",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_52() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="XXGenera la estructura de directorios y archivos boilerplate para un nuevo conector.XX",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_53() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_54() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="GENERA LA ESTRUCTURA DE DIRECTORIOS Y ARCHIVOS BOILERPLATE PARA UN NUEVO CONECTOR.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_55() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument(None, help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_56() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help=None)
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_57() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument(help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_58() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", )
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_59() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("XXnameXX", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_60() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("NAME", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_61() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="XXNombre del conector (snake_case, ej: mi_conector)XX")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_62() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_63() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="NOMBRE DEL CONECTOR (SNAKE_CASE, EJ: MI_CONECTOR)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_64() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument(None, default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_65() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default=None, help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_66() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help=None)
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_67() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument(default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_68() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_69() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", )
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_70() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("XX--categoryXX", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_71() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--CATEGORY", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_72() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="XXgeneralXX", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_73() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="GENERAL", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_74() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="XXCategoria del conector (default: general)XX")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_75() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_76() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="CATEGORIA DEL CONECTOR (DEFAULT: GENERAL)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_77() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument(None, default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_78() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default=None, choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_79() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=None,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_80() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help=None)

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_81() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument(default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_82() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_83() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_84() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             )

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_85() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("XX--auth-typeXX", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_86() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--AUTH-TYPE", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_87() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="XXnoneXX", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_88() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="NONE", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_89() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="XXTipo de autenticacion del conector (default: none)XX")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_90() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_91() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="TIPO DE AUTENTICACION DEL CONECTOR (DEFAULT: NONE)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_92() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = None
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_93() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        None,
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_94() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help=None,
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_95() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description=None,
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_96() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_97() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_98() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_99() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "XXtestXX",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_100() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "TEST",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_101() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="XXEjecuta un conector en entorno sandboxXX",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_102() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_103() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="EJECUTA UN CONECTOR EN ENTORNO SANDBOX",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_104() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="XXImporta, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.XX",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_105() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_106() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="IMPORTA, INSTANCIA Y EJECUTA UN CONECTOR EN UN ENTORNO AISLADO CAPTURANDO RESULTADOS Y ERRORES.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_107() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument(None, help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_108() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help=None)
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_109() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument(help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_110() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", )
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_111() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("XXconnector_pathXX", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_112() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("CONNECTOR_PATH", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_113() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="XXRuta al directorio del conectorXX")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_114() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_115() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="RUTA AL DIRECTORIO DEL CONECTOR")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_116() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument(None, default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_117() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default=None, help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_118() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help=None)
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_119() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument(default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_120() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_121() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", )
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_122() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("XX--actionXX", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_123() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--ACTION", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_124() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="XXpingXX", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_125() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="PING", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_126() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="XXAccion a ejecutar (default: ping)XX")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_127() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_128() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="ACCION A EJECUTAR (DEFAULT: PING)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_129() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument(None, default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_130() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help=None)

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_131() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument(default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_132() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_133() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             )

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_134() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("XX--inputXX", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_135() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--INPUT", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_136() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="XXParametros de entrada como JSON string o ruta a archivo JSONXX")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_137() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="parametros de entrada como json string o ruta a archivo json")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_138() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="PARAMETROS DE ENTRADA COMO JSON STRING O RUTA A ARCHIVO JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_139() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = None
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_140() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        None,
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_141() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help=None,
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_142() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description=None,
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_143() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_144() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_145() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_146() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "XXvalidateXX",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_147() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "VALIDATE",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_148() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="XXValida estructura y esquema del conectorXX",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_149() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_150() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="VALIDA ESTRUCTURA Y ESQUEMA DEL CONECTOR",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_151() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="XXVerifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.XX",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_152() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_153() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="VERIFICA QUE EL CONECTOR CUMPLA CON TODOS LOS REQUISITOS: ARCHIVOS, HERENCIA, METODOS, ESQUEMA Y AUTH.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_154() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument(None, help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_155() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help=None)

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_156() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument(help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_157() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", )

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_158() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("XXconnector_pathXX", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_159() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("CONNECTOR_PATH", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_160() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="XXRuta al directorio del conectorXX")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_161() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_162() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="RUTA AL DIRECTORIO DEL CONECTOR")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_163() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = None
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_164() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        None,
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_165() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help=None,
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_166() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description=None,
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_167() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_168() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_169() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_170() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "XXpublishXX",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_171() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "PUBLISH",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_172() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="XXEmpaqueta y publica al marketplaceXX",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_173() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_174() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="EMPAQUETA Y PUBLICA AL MARKETPLACE",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_175() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="XXValida, empaqueta como .zip y publica el conector al registro del marketplace.XX",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_176() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_177() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="VALIDA, EMPAQUETA COMO .ZIP Y PUBLICA EL CONECTOR AL REGISTRO DEL MARKETPLACE.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_178() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument(None, help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_179() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help=None)
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_180() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument(help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_181() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", )
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_182() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("XXconnector_pathXX", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_183() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("CONNECTOR_PATH", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_184() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="XXRuta al directorio del conectorXX")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_185() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_186() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="RUTA AL DIRECTORIO DEL CONECTOR")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_187() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument(None, default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_188() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help=None)

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_189() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument(default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_190() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_191() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                )

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_192() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("XX--registryXX", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_193() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--REGISTRY", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_194() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="XXURL del registro del marketplaceXX")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_195() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="url del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_196() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL DEL REGISTRO DEL MARKETPLACE")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_197() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = None
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_198() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        None,
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_199() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help=None,
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_200() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description=None,
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_201() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_202() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_203() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_204() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "XXversionXX",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_205() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "VERSION",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_206() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="XXGestiona la version del conectorXX",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_207() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_208() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="GESTIONA LA VERSION DEL CONECTOR",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_209() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="XXMuestra o actualiza la version del conector siguiendo semver.XX",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_210() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_211() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="MUESTRA O ACTUALIZA LA VERSION DEL CONECTOR SIGUIENDO SEMVER.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_212() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument(None, help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_213() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help=None)
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_214() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument(help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_215() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", )
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_216() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("XXconnector_pathXX", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_217() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("CONNECTOR_PATH", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_218() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="XXRuta al directorio del conectorXX")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_219() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_220() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="RUTA AL DIRECTORIO DEL CONECTOR")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_221() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument(None, choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_222() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=None, default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_223() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help=None)

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_224() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument(choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_225() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_226() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_227() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                )

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_228() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("XX--bumpXX", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_229() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--BUMP", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_230() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["XXmajorXX", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_231() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["MAJOR", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_232() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "XXminorXX", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_233() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "MINOR", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_234() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "XXpatchXX"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_235() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "PATCH"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_236() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="XXIncrementa la version (major|minor|patch)XX")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_237() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_238() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="INCREMENTA LA VERSION (MAJOR|MINOR|PATCH)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_239() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        None,
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_240() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help=None,
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_241() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description=None,
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_242() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_243() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_244() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_245() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "XXlistXX",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_246() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "LIST",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_247() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="XXLista todos los conectores registradosXX",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_248() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_249() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="LISTA TODOS LOS CONECTORES REGISTRADOS",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_250() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="XXMuestra una tabla con nombre, version, categoria y estado de cada conector registrado.XX",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_251() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_252() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="MUESTRA UNA TABLA CON NOMBRE, VERSION, CATEGORIA Y ESTADO DE CADA CONECTOR REGISTRADO.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_253() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = None
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_254() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        None,
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_255() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help=None,
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_256() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description=None,
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_257() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_258() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_259() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_260() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "XXinfoXX",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_261() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "INFO",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_262() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="XXMuestra informacion detallada del conectorXX",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_263() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_264() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="MUESTRA INFORMACION DETALLADA DEL CONECTOR",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_265() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="XXMuestra metadata, acciones, requisitos de autenticacion y esquema del conector.XX",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_266() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_267() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="MUESTRA METADATA, ACCIONES, REQUISITOS DE AUTENTICACION Y ESQUEMA DEL CONECTOR.",
    )
    info_parser.add_argument("connector_name", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_268() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument(None, help="Nombre del conector")

    return parser


def x_build_parser__mutmut_269() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help=None)

    return parser


def x_build_parser__mutmut_270() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument(help="Nombre del conector")

    return parser


def x_build_parser__mutmut_271() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", )

    return parser


def x_build_parser__mutmut_272() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("XXconnector_nameXX", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_273() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("CONNECTOR_NAME", help="Nombre del conector")

    return parser


def x_build_parser__mutmut_274() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="XXNombre del conectorXX")

    return parser


def x_build_parser__mutmut_275() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="nombre del conector")

    return parser


def x_build_parser__mutmut_276() -> argparse.ArgumentParser:
    """
    Construye el parser de argumentos principal del CLI.

    Configura todos los subcomandos con sus argumentos y opciones.

    Retorna:
        ArgumentParser configurado con todos los subcomandos
    """
    parser = argparse.ArgumentParser(
        prog="zenic",
        description="Zenic CLI — Herramienta de desarrollo de conectores para Zenic-Flijo",
        epilog="Use 'zenic <comando> --help' para mas informacion sobre cada comando.",
    )

    parser.add_argument(
        "--version",
        action="version",
        version=f"%(prog)s {CLI_VERSION}",
    )

    subparsers = parser.add_subparsers(
        title="comandos",
        dest="command",
        help="Comando a ejecutar",
    )

    # ── init ───────────────────────────────────────────────────
    init_parser = subparsers.add_parser(
        "init",
        help="Crea el scaffolding de un nuevo conector",
        description="Genera la estructura de directorios y archivos boilerplate para un nuevo conector.",
    )
    init_parser.add_argument("name", help="Nombre del conector (snake_case, ej: mi_conector)")
    init_parser.add_argument("--category", default="general", help="Categoria del conector (default: general)")
    init_parser.add_argument("--auth-type", default="none", choices=VALID_AUTH_TYPES,
                             help="Tipo de autenticacion del conector (default: none)")

    # ── test ───────────────────────────────────────────────────
    test_parser = subparsers.add_parser(
        "test",
        help="Ejecuta un conector en entorno sandbox",
        description="Importa, instancia y ejecuta un conector en un entorno aislado capturando resultados y errores.",
    )
    test_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    test_parser.add_argument("--action", default="ping", help="Accion a ejecutar (default: ping)")
    test_parser.add_argument("--input", default=None,
                             help="Parametros de entrada como JSON string o ruta a archivo JSON")

    # ── validate ───────────────────────────────────────────────
    validate_parser = subparsers.add_parser(
        "validate",
        help="Valida estructura y esquema del conector",
        description="Verifica que el conector cumpla con todos los requisitos: archivos, herencia, metodos, esquema y auth.",
    )
    validate_parser.add_argument("connector_path", help="Ruta al directorio del conector")

    # ── publish ────────────────────────────────────────────────
    publish_parser = subparsers.add_parser(
        "publish",
        help="Empaqueta y publica al marketplace",
        description="Valida, empaqueta como .zip y publica el conector al registro del marketplace.",
    )
    publish_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    publish_parser.add_argument("--registry", default=None,
                                help="URL del registro del marketplace")

    # ── version ────────────────────────────────────────────────
    version_parser = subparsers.add_parser(
        "version",
        help="Gestiona la version del conector",
        description="Muestra o actualiza la version del conector siguiendo semver.",
    )
    version_parser.add_argument("connector_path", help="Ruta al directorio del conector")
    version_parser.add_argument("--bump", choices=["major", "minor", "patch"], default=None,
                                help="Incrementa la version (major|minor|patch)")

    # ── list ───────────────────────────────────────────────────
    subparsers.add_parser(
        "list",
        help="Lista todos los conectores registrados",
        description="Muestra una tabla con nombre, version, categoria y estado de cada conector registrado.",
    )

    # ── info ───────────────────────────────────────────────────
    info_parser = subparsers.add_parser(
        "info",
        help="Muestra informacion detallada del conector",
        description="Muestra metadata, acciones, requisitos de autenticacion y esquema del conector.",
    )
    info_parser.add_argument("connector_name", help="NOMBRE DEL CONECTOR")

    return parser

mutants_x_build_parser__mutmut['_mutmut_orig'] = x_build_parser__mutmut_orig # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_1'] = x_build_parser__mutmut_1 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_2'] = x_build_parser__mutmut_2 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_3'] = x_build_parser__mutmut_3 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_4'] = x_build_parser__mutmut_4 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_5'] = x_build_parser__mutmut_5 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_6'] = x_build_parser__mutmut_6 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_7'] = x_build_parser__mutmut_7 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_8'] = x_build_parser__mutmut_8 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_9'] = x_build_parser__mutmut_9 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_10'] = x_build_parser__mutmut_10 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_11'] = x_build_parser__mutmut_11 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_12'] = x_build_parser__mutmut_12 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_13'] = x_build_parser__mutmut_13 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_14'] = x_build_parser__mutmut_14 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_15'] = x_build_parser__mutmut_15 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_16'] = x_build_parser__mutmut_16 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_17'] = x_build_parser__mutmut_17 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_18'] = x_build_parser__mutmut_18 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_19'] = x_build_parser__mutmut_19 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_20'] = x_build_parser__mutmut_20 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_21'] = x_build_parser__mutmut_21 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_22'] = x_build_parser__mutmut_22 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_23'] = x_build_parser__mutmut_23 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_24'] = x_build_parser__mutmut_24 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_25'] = x_build_parser__mutmut_25 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_26'] = x_build_parser__mutmut_26 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_27'] = x_build_parser__mutmut_27 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_28'] = x_build_parser__mutmut_28 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_29'] = x_build_parser__mutmut_29 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_30'] = x_build_parser__mutmut_30 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_31'] = x_build_parser__mutmut_31 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_32'] = x_build_parser__mutmut_32 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_33'] = x_build_parser__mutmut_33 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_34'] = x_build_parser__mutmut_34 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_35'] = x_build_parser__mutmut_35 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_36'] = x_build_parser__mutmut_36 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_37'] = x_build_parser__mutmut_37 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_38'] = x_build_parser__mutmut_38 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_39'] = x_build_parser__mutmut_39 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_40'] = x_build_parser__mutmut_40 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_41'] = x_build_parser__mutmut_41 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_42'] = x_build_parser__mutmut_42 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_43'] = x_build_parser__mutmut_43 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_44'] = x_build_parser__mutmut_44 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_45'] = x_build_parser__mutmut_45 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_46'] = x_build_parser__mutmut_46 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_47'] = x_build_parser__mutmut_47 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_48'] = x_build_parser__mutmut_48 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_49'] = x_build_parser__mutmut_49 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_50'] = x_build_parser__mutmut_50 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_51'] = x_build_parser__mutmut_51 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_52'] = x_build_parser__mutmut_52 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_53'] = x_build_parser__mutmut_53 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_54'] = x_build_parser__mutmut_54 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_55'] = x_build_parser__mutmut_55 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_56'] = x_build_parser__mutmut_56 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_57'] = x_build_parser__mutmut_57 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_58'] = x_build_parser__mutmut_58 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_59'] = x_build_parser__mutmut_59 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_60'] = x_build_parser__mutmut_60 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_61'] = x_build_parser__mutmut_61 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_62'] = x_build_parser__mutmut_62 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_63'] = x_build_parser__mutmut_63 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_64'] = x_build_parser__mutmut_64 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_65'] = x_build_parser__mutmut_65 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_66'] = x_build_parser__mutmut_66 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_67'] = x_build_parser__mutmut_67 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_68'] = x_build_parser__mutmut_68 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_69'] = x_build_parser__mutmut_69 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_70'] = x_build_parser__mutmut_70 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_71'] = x_build_parser__mutmut_71 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_72'] = x_build_parser__mutmut_72 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_73'] = x_build_parser__mutmut_73 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_74'] = x_build_parser__mutmut_74 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_75'] = x_build_parser__mutmut_75 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_76'] = x_build_parser__mutmut_76 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_77'] = x_build_parser__mutmut_77 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_78'] = x_build_parser__mutmut_78 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_79'] = x_build_parser__mutmut_79 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_80'] = x_build_parser__mutmut_80 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_81'] = x_build_parser__mutmut_81 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_82'] = x_build_parser__mutmut_82 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_83'] = x_build_parser__mutmut_83 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_84'] = x_build_parser__mutmut_84 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_85'] = x_build_parser__mutmut_85 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_86'] = x_build_parser__mutmut_86 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_87'] = x_build_parser__mutmut_87 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_88'] = x_build_parser__mutmut_88 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_89'] = x_build_parser__mutmut_89 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_90'] = x_build_parser__mutmut_90 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_91'] = x_build_parser__mutmut_91 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_92'] = x_build_parser__mutmut_92 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_93'] = x_build_parser__mutmut_93 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_94'] = x_build_parser__mutmut_94 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_95'] = x_build_parser__mutmut_95 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_96'] = x_build_parser__mutmut_96 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_97'] = x_build_parser__mutmut_97 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_98'] = x_build_parser__mutmut_98 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_99'] = x_build_parser__mutmut_99 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_100'] = x_build_parser__mutmut_100 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_101'] = x_build_parser__mutmut_101 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_102'] = x_build_parser__mutmut_102 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_103'] = x_build_parser__mutmut_103 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_104'] = x_build_parser__mutmut_104 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_105'] = x_build_parser__mutmut_105 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_106'] = x_build_parser__mutmut_106 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_107'] = x_build_parser__mutmut_107 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_108'] = x_build_parser__mutmut_108 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_109'] = x_build_parser__mutmut_109 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_110'] = x_build_parser__mutmut_110 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_111'] = x_build_parser__mutmut_111 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_112'] = x_build_parser__mutmut_112 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_113'] = x_build_parser__mutmut_113 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_114'] = x_build_parser__mutmut_114 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_115'] = x_build_parser__mutmut_115 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_116'] = x_build_parser__mutmut_116 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_117'] = x_build_parser__mutmut_117 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_118'] = x_build_parser__mutmut_118 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_119'] = x_build_parser__mutmut_119 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_120'] = x_build_parser__mutmut_120 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_121'] = x_build_parser__mutmut_121 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_122'] = x_build_parser__mutmut_122 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_123'] = x_build_parser__mutmut_123 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_124'] = x_build_parser__mutmut_124 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_125'] = x_build_parser__mutmut_125 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_126'] = x_build_parser__mutmut_126 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_127'] = x_build_parser__mutmut_127 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_128'] = x_build_parser__mutmut_128 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_129'] = x_build_parser__mutmut_129 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_130'] = x_build_parser__mutmut_130 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_131'] = x_build_parser__mutmut_131 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_132'] = x_build_parser__mutmut_132 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_133'] = x_build_parser__mutmut_133 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_134'] = x_build_parser__mutmut_134 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_135'] = x_build_parser__mutmut_135 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_136'] = x_build_parser__mutmut_136 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_137'] = x_build_parser__mutmut_137 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_138'] = x_build_parser__mutmut_138 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_139'] = x_build_parser__mutmut_139 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_140'] = x_build_parser__mutmut_140 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_141'] = x_build_parser__mutmut_141 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_142'] = x_build_parser__mutmut_142 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_143'] = x_build_parser__mutmut_143 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_144'] = x_build_parser__mutmut_144 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_145'] = x_build_parser__mutmut_145 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_146'] = x_build_parser__mutmut_146 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_147'] = x_build_parser__mutmut_147 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_148'] = x_build_parser__mutmut_148 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_149'] = x_build_parser__mutmut_149 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_150'] = x_build_parser__mutmut_150 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_151'] = x_build_parser__mutmut_151 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_152'] = x_build_parser__mutmut_152 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_153'] = x_build_parser__mutmut_153 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_154'] = x_build_parser__mutmut_154 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_155'] = x_build_parser__mutmut_155 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_156'] = x_build_parser__mutmut_156 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_157'] = x_build_parser__mutmut_157 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_158'] = x_build_parser__mutmut_158 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_159'] = x_build_parser__mutmut_159 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_160'] = x_build_parser__mutmut_160 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_161'] = x_build_parser__mutmut_161 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_162'] = x_build_parser__mutmut_162 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_163'] = x_build_parser__mutmut_163 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_164'] = x_build_parser__mutmut_164 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_165'] = x_build_parser__mutmut_165 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_166'] = x_build_parser__mutmut_166 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_167'] = x_build_parser__mutmut_167 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_168'] = x_build_parser__mutmut_168 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_169'] = x_build_parser__mutmut_169 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_170'] = x_build_parser__mutmut_170 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_171'] = x_build_parser__mutmut_171 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_172'] = x_build_parser__mutmut_172 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_173'] = x_build_parser__mutmut_173 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_174'] = x_build_parser__mutmut_174 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_175'] = x_build_parser__mutmut_175 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_176'] = x_build_parser__mutmut_176 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_177'] = x_build_parser__mutmut_177 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_178'] = x_build_parser__mutmut_178 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_179'] = x_build_parser__mutmut_179 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_180'] = x_build_parser__mutmut_180 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_181'] = x_build_parser__mutmut_181 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_182'] = x_build_parser__mutmut_182 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_183'] = x_build_parser__mutmut_183 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_184'] = x_build_parser__mutmut_184 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_185'] = x_build_parser__mutmut_185 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_186'] = x_build_parser__mutmut_186 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_187'] = x_build_parser__mutmut_187 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_188'] = x_build_parser__mutmut_188 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_189'] = x_build_parser__mutmut_189 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_190'] = x_build_parser__mutmut_190 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_191'] = x_build_parser__mutmut_191 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_192'] = x_build_parser__mutmut_192 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_193'] = x_build_parser__mutmut_193 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_194'] = x_build_parser__mutmut_194 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_195'] = x_build_parser__mutmut_195 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_196'] = x_build_parser__mutmut_196 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_197'] = x_build_parser__mutmut_197 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_198'] = x_build_parser__mutmut_198 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_199'] = x_build_parser__mutmut_199 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_200'] = x_build_parser__mutmut_200 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_201'] = x_build_parser__mutmut_201 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_202'] = x_build_parser__mutmut_202 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_203'] = x_build_parser__mutmut_203 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_204'] = x_build_parser__mutmut_204 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_205'] = x_build_parser__mutmut_205 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_206'] = x_build_parser__mutmut_206 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_207'] = x_build_parser__mutmut_207 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_208'] = x_build_parser__mutmut_208 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_209'] = x_build_parser__mutmut_209 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_210'] = x_build_parser__mutmut_210 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_211'] = x_build_parser__mutmut_211 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_212'] = x_build_parser__mutmut_212 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_213'] = x_build_parser__mutmut_213 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_214'] = x_build_parser__mutmut_214 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_215'] = x_build_parser__mutmut_215 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_216'] = x_build_parser__mutmut_216 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_217'] = x_build_parser__mutmut_217 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_218'] = x_build_parser__mutmut_218 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_219'] = x_build_parser__mutmut_219 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_220'] = x_build_parser__mutmut_220 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_221'] = x_build_parser__mutmut_221 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_222'] = x_build_parser__mutmut_222 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_223'] = x_build_parser__mutmut_223 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_224'] = x_build_parser__mutmut_224 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_225'] = x_build_parser__mutmut_225 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_226'] = x_build_parser__mutmut_226 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_227'] = x_build_parser__mutmut_227 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_228'] = x_build_parser__mutmut_228 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_229'] = x_build_parser__mutmut_229 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_230'] = x_build_parser__mutmut_230 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_231'] = x_build_parser__mutmut_231 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_232'] = x_build_parser__mutmut_232 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_233'] = x_build_parser__mutmut_233 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_234'] = x_build_parser__mutmut_234 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_235'] = x_build_parser__mutmut_235 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_236'] = x_build_parser__mutmut_236 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_237'] = x_build_parser__mutmut_237 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_238'] = x_build_parser__mutmut_238 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_239'] = x_build_parser__mutmut_239 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_240'] = x_build_parser__mutmut_240 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_241'] = x_build_parser__mutmut_241 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_242'] = x_build_parser__mutmut_242 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_243'] = x_build_parser__mutmut_243 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_244'] = x_build_parser__mutmut_244 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_245'] = x_build_parser__mutmut_245 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_246'] = x_build_parser__mutmut_246 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_247'] = x_build_parser__mutmut_247 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_248'] = x_build_parser__mutmut_248 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_249'] = x_build_parser__mutmut_249 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_250'] = x_build_parser__mutmut_250 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_251'] = x_build_parser__mutmut_251 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_252'] = x_build_parser__mutmut_252 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_253'] = x_build_parser__mutmut_253 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_254'] = x_build_parser__mutmut_254 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_255'] = x_build_parser__mutmut_255 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_256'] = x_build_parser__mutmut_256 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_257'] = x_build_parser__mutmut_257 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_258'] = x_build_parser__mutmut_258 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_259'] = x_build_parser__mutmut_259 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_260'] = x_build_parser__mutmut_260 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_261'] = x_build_parser__mutmut_261 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_262'] = x_build_parser__mutmut_262 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_263'] = x_build_parser__mutmut_263 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_264'] = x_build_parser__mutmut_264 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_265'] = x_build_parser__mutmut_265 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_266'] = x_build_parser__mutmut_266 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_267'] = x_build_parser__mutmut_267 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_268'] = x_build_parser__mutmut_268 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_269'] = x_build_parser__mutmut_269 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_270'] = x_build_parser__mutmut_270 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_271'] = x_build_parser__mutmut_271 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_272'] = x_build_parser__mutmut_272 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_273'] = x_build_parser__mutmut_273 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_274'] = x_build_parser__mutmut_274 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_275'] = x_build_parser__mutmut_275 # type: ignore # mutmut generated
mutants_x_build_parser__mutmut['x_build_parser__mutmut_276'] = x_build_parser__mutmut_276 # type: ignore # mutmut generated
